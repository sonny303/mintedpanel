// US state codes used across settings dialogs. Canonical list promoted to
// src/lib/usStates.ts (E1.1 TE-5); re-exported here so existing settings
// imports keep working.
export { US_STATES } from "@/lib/usStates";
