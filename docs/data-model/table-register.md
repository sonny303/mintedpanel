# Table Register

The living inventory of every table in the public schema: its layer in the data
model, its lifecycle status, and its replacement when superseded. This is the
reconciliation surface for the planning harness (chatprd → reviewer → dev):

- **Epic authors / chatprd**: every epic lists a _table trace_ — the tables its
  screens and API calls read and write. Reconcile the trace against this
  register at design time; a core table absent from a stage's traces must be a
  deliberate decision, not an omission.
- **Reviewer**: check the epic's table trace against the register and the diff.
  Any migration PR that adds or supersedes a table/column updates the register
  row in the same PR.
- **Stage gates**: at each stage promotion, re-run the usage audit (grep
  `from("<table>")` across `src/`, excluding tests) and diff against the
  register. A table whose usage drops to zero is re-statused deliberately or
  the drop is a redesign bug.

## Status vocabulary

- **core** — the model's spine; epics must account for it.
- **support** — real but peripheral; used by specific features.
- **frozen-mirror** — legacy columns/tables kept per the additive rule;
  readable, never the source of truth. Must name its replacement.
- **dormant** — no code reads or writes it; kept for history/backup.
- **dead** — no code usage and no data value; never reference again.

## The data model in one paragraph

`credential_cases` is the center: one row per provider × group × payer × state
(an application per customer × entity × product × jurisdiction; legacy
NULL-group rows stay unique at the old provider × payer × state grain). The
party layer
(groups, providers, facilities, licenses, payers, MSOs) feeds it; `contracts`
(group × payer × state) is the entity-level agreement a case rides on (a value
join on that triple — there is no FK); the ledger layer (`touches`,
`status_history`, `audit_log`, `communication_event`) is the append-only
journal of everything that happened to it. Current status columns are
materialized balances; the ledger is the transaction log that produced them.

## Register (audited 2026-07-10)

Usage = non-test files in `src/` referencing the table (services, server
routes, lib). RPC-mediated tables note the RPC.

| Table | Layer | Status | Usage | Notes / replacement |
| ----------------------------- | --------- | ---------------------------------------------- | ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| organizations | tenant | core | 2 | + `create_organization` RPC |
| memberships | tenant | core | 6 | the M:N house pattern |
| profiles | tenant | core | 7 | mirrors `auth.users` |
| pending_invites | tenant | support | 1 + `claim_invites` RPC | RPC-mediated by design |
| party_capture_links | tenant | support | 1 | one-time owner capture links (E0.5); org-scoped; token stored as hash only |
| inbound_leads | tenant | support | 1 | public contact-us leads (E0.5); intentionally NOT org-scoped (leads pre-date any org) |
| report_shares | tenant | support | 1 | tokenized read-only portfolio/report shares (E0.6); scoped by `created_by = auth.uid()` |
| provider_groups | party | core | 3 | sprawl target: 30 purpose-keyed contact/address columns → planned `group_addresses` / `group_contacts` child tables |
| providers | party | core | 4 | `license_*` mirrors `state_licenses` (child table is truth); `malpractice_*` → planned `provider_insurance_policies`; `launch_id` column is dead; `verification_state` (E3.1 TE-1, `verified` default / `pending_verification`) fences bulk-imported rows out of E1.8 readiness + E2.0 generation candidacy via the single `listProviderReadinessFacts` read until explicitly verified — distinct from `reference_only` and from `status`; **E4.2** `is_test_provider` (F4.2.7 designated dry-run test provider; excluded from queue/generation/scorecard via the shared testProvider predicate + the readiness-facts fence) |
| facilities | party | core | 5 | absorbed the launches concept (location status track) |
| provider_facility_assignments | party | core | 4 | template for `provider_group_assignments` (built E1.3); `provider_id`/`facility_id` NOT-NULL + single `is_primary` per provider enforced (E0.10); `start_date` NOT-NULL floor `NOT VALID` (E1.4 — validate after legacy-null remediation); atomic primary swap via `set_primary_assignment` RPC (E1.4) |
| provider_group_assignments | party | core | 1 | M:N provider↔group join (E1.3, mirrors the pfa template); unique (provider, group) + single `is_primary` per provider; `providers.group_id` is the frozen primary mirror — no new readers |
| state_licenses | party | core | 4 | source of truth for licensure; `provider_id` NOT-NULL + `state` format enforced (E0.10); + PSV trail columns `verified_status/verified_at/verified_by/verification_source_url` (E1.3) |
| provider_ssn_vault | party | support | 0 (RPC-only) | **E4.4** the server-only Sensitive Identifiers Vault (`20260717120000`): the full SSN encrypted at rest (`ssn_ciphertext bytea` + `algo`/`key_version` Option-B-ready columns), one row per provider (`provider_id` PK). ZERO-TRUST: RLS on, `REVOKE ALL` from anon+authenticated, NO client SELECT grant and NO service_role grant — the ONLY access is the SECURITY DEFINER RPCs `store_ssn` (writer), `reveal_ssn` (admin + justification, audited READ), `release_ssn_for_fill` (service_role-only, /api fill path, audited READ), `submit_ssn_intake` (anon, via the intake link). In-DB pgcrypto (`pgp_sym_encrypt/decrypt`) with the master key from a server GUC (`app.settings.ssn_vault_key`, operator-provisioned). `providers.ssn_last4` stays the ONLY value any ordinary read/list/export/API returns (mask `***--1234`). |
| provider_ssn_intake_links | tenant | support | 1 | **E4.4** secure SSN intake links (`20260717120100`, E0.5 capture-link pattern): hashed token only, single active per provider (partial unique), 72h expiry, `state active\|used\|expired\|revoked`. Member SELECT RLS; all writes via RPC (`create_ssn_intake_link` writer, `validate_ssn_intake_token`/`submit_ssn_intake` anon + E0.8 throttled). Carries NO SSN — only the token machinery; the value encrypts straight into `provider_ssn_vault` on submit. |
| payers | party | core | 3 | org-scoped + global catalog (`org_id` NULL); E1.6 identity columns (`payer_kind`, `status`, `aliases[]`, `states[]`, `payer_slug` = canonical dataset key + partial unique `uq_payers_payer_slug` (the seed identity/dedupe key; clearinghouse IDs dropped per [e1.6]), dormant `merged_into_id`); **E4.2** `resolution_id_label`/`resolution_id_expected` (F4.2.1 resolution-identifier config — since the governance PR these are the MINTED-CURATED GLOBAL FALLBACK tier only; the org-varying override lives in `org_payer_settings`, both read through the E4.0 payerResolutionIdentifier seam). **Pre-GA drop (`20260717221914`, superseding the never-applied `20260716180000`/PR #169):** removed the dead curated-billing block (`provisional_billing_allowed/notes`, `retro_billing_allowed/window_days`, `caqh_pull_deadline_days`, `provider_type_path`, `prior_auth_vendor`, `payer_billing_id`, `portal_url`) + `cms_hios_id` + `prerequisite_payer_id` — audit classes 2/7, no consumer (see `docs/data-model/payer-field-usage-audit.md`). **E4.2 governance:** org free-text payer creation removed (identities are SELECTED from the catalog, never typed — no INSERT path in `payers.ts`); global rows org-read-only. **2026-07-18 close-out (`20260718120000_payers_org_write_lockdown`):** legacy org-scoped rows are GONE (pre-prod-cut wipe 2026-07-17; pre-wipe data in the `mintedpanel-backup-july17` project) and the org INSERT/UPDATE policies+grants were revoked — the table is global-catalog-only and member-SELECT-only from the app (`payers.ts` is read-only; `updatePayer` deleted); catalog writes stay service-role. Cutover record closed as superseded (`docs/data-model/legacy-payer-cutover.md`). **E6.5:** additive `delegation_note` — curated delegation fact replacing the retired MSO routing engine (platform-written only, no app writer — superseded by E6.7 below). **E6.7 (`20260727120000_e67_payer_manual_setup`, repo + hosted): the manual-setup enabler — the table has ONE app write path again, the `create_payer`/`update_payer` SECURITY DEFINER RPCs** (authenticated, anon rejected in-body, writer-member of the caller org, audited; the `author_global_sop` interim posture — R7 hardens). Direct table DML stays revoked (the 20260718120000 lockdown stands). Additive columns: the **ID-expectation split** `group_id_label`/`group_id_expected` + `provider_id_label`/`provider_id_expected` (provider pair backfilled from the legacy `resolution_id_*` pair, which DEPRECATES IN PLACE — stop-write; the payerResolutionIdentifier seam reads provider pair → legacy pair → generic, and `set_case_status` requires exactly the expected IDs at Approved); **provenance** `created_by`/`source (seed\|sync\|manual, existing rows backfilled 'sync', CHECK)`/`updated_at` — RPC-stamped; partial unique **`uq_payers_global_normalized_name` on `lower(btrim(name)) WHERE org_id IS NULL AND status <> 'merged'`** (the DB dup backstop under the RPCs' name+alias guard; live data verified collision-free pre-index). Manual rows are GLOBAL (`org_id NULL`, PM decision — authored once, template inheritance intact), `payer_slug`/`last_synced_at` NULL. `delegation_note` is now user-entered through the RPC seam ONLY (payerGovernance.test.ts pins the single writer). **E6.7 PR 2 (F6.7.4/F6.7.5, no schema change): the catalog sync is RETIRED** (`scripts/payer-catalog-sync.mjs` deleted; the reference dataset README is frozen) — `payer_slug` + `last_synced_at` are DEPRECATED IN PLACE (stop-write; the seeded values stay per the additive rule, nothing writes them anymore), and stored `avg_decision_days` lost its only writer with the sync so the UI stopped rendering it (column stays; TECH-DEBT logs the derive-from-case-outcomes replacement). **E6.8 (`20260727150000`/`150100`, repo + hosted): the lifecycle enabler** — additive **`archived_at timestamptz NULL`** (the reversible org-workflow archive flag, NOT the platform `status` domain; the normalized-name dup backstop still covers archived rows) set/cleared ONLY by the **`archive_payer`/`reactivate_payer` SECURITY DEFINER RPCs** (authenticated, anon rejected in-body, writer-member, audited; archive REJECTS with `payer_archive_open_cases: <n>` while ANY org holds an open non-terminal case on the payer — nothing written). Archived exclusion from attach eligibility / generation candidates / the default payer-list derivation is CLIENT-side filtering on `archived_at IS NULL` (no RLS change — closed cases keep resolving the payer name; Show-archived = UI slice A). **`merge_payer(p_org_id, p_loser, p_survivor)`** is the in-app duplicate repair (one transaction, all-or-nothing): re-points `sop_templates`/`payer_network_targets`/`enrollment_facts`/OPEN `credential_cases`/`org_payer_assignments` to the survivor (collision semantics per row — see those tables), appends the loser's name to the survivor's `aliases[]`, marks the loser `status 'merged'` + `merged_into_id`; a 4-part case-key collision RAISEs `payer_merge_case_conflict` listing the case numbers and commits NOTHING. Not undoable from the app; `payer_contacts`/`contracts`/`case_generation_exclusions` deliberately stay on the loser (outside the epic trace). **3M Slice 6 D6.1 (`20260809120000_slice6_create_payer_assign_flag`, repo-only — hosted apply = operator step): `create_payer` gained `p_assign_to_org boolean DEFAULT true`** (old 10-arg signature DROPPED and recreated — the E4.2/E6.8 no-overload precedent, since a defaulted 11th arg would leave both resolvable for PostgREST). `true` = the unchanged E6.7 behaviour (identity insert + the caller org's `org_payer_assignments` upsert + the same audit row, one transaction); `false` = the GLOBAL identity ALONE, no assignment — platform authoring (payer → SOP → shared portal → train → map) ahead of any org adopting it. `p_org_id` stays REQUIRED either way: it authorizes the call (writer-member) and owns the audit row — no platform role is invented (R7). The audit `after` payload carries `assignedToOrg` and the description distinguishes the two intents, which is the ONLY record of an unassigned create (it leaves no assignment row to read). **OPA-RETIRE (`20260810220000`):** `payers_select` widened to own-org OR `org_id IS NULL` (no assignment subquery); `create_payer` stops upserting `org_payer_assignments` (catalog create only; adoption = group attach) |
| payer_contacts | party | support | 1 | **E6.7 F6.7.2a (`20260727120200`, repo + hosted)** — operational contacts on a payer, a CHILD table at the purpose grain (SCHEMA.md grain rule; never columns on `payers`): `purpose` CHECK `credentialing\|enrollment\|escalation\|general`, name/email/phone/note, `is_default` + partial `UNIQUE (payer_id, purpose) WHERE is_default` (one default per purpose — the future template email-step resolution target, resolved by `(payer_id, purpose)` at resolution time, never a hard FK), reachability CHECK (email OR phone). Read = member SELECT via the PARENT payer's visibility (restated payers disjunct — own-org OR assigned-global); writes ONLY via the audited `upsert_payer_contact`/`delete_payer_contact` RPCs (authenticated, writer-member, default-swap in-RPC; hard delete allowed — operational data, not a ledger); clients hold SELECT only (revoke-then-grant floor). Service `src/services/payerContacts.ts` → `usePayerContacts.ts`; no rendered UI in E6.7 (the Payer Detail design consumes it later) |
| payer_catalog_changes | party | core | 0 (dormant) | E1.6 append-only catalog diff log (sync/manual → unreviewed/accepted/rejected). **E4.2 governance (`20260716191000`): PLATFORM-ONLY** — authenticated SELECT policy dropped + ALL authenticated grants revoked, and `review_payer_catalog_change` EXECUTE revoked from authenticated (its reissued body also rejects org-user JWTs; service_role keeps full access). The in-app review panel was removed. **E6.7 PR 2 (F6.7.4): DORMANT** — the catalog sync (its only diff writer) is retired, so nothing writes here anymore; table + review RPC kept per the additive rule (service-role/MCP access unchanged if a platform diff is ever recorded by hand) |
| org_payer_assignments | party | core | 0 (dormant-as-gate) | catalog subscription join historically gated payers visibility + attach WITH CHECK + create_payer auto-upsert. E4.2 lifecycle (`status active\|archived` + `archived_at`, `archive_org_payer_assignment` RPC) and E6.8 `merge_payer` re-point/fold still exist on the table. **OPA-RETIRE (R1 B, `20260810220000`, repo-only — hosted apply = operator step):** RETIRED AS A GATE ONLY — never DROP (additive rule). App "in my network" = ≥1 active `payer_network_targets` row; `payers_select` admits every global; targets WITH CHECK drops the assignment EXISTS; `create_payer` no longer upserts a row (and never resurrects `p_assign_to_org`). Service `orgPayerAssignments.ts` / hooks remain for residual/legacy callers but no product surface gates on them. Rows stay for purge/merge history |
| org_payer_settings | party | support | 1 | **E4.2 governance (`20260716190000`)** — the org × payer configuration grain (grain O): global payer facts stay Minted-curated on `payers`; what an ORG configures about a payer lives here. `UNIQUE (org_id, payer_id)`; carries ONLY `resolution_id_label`/`resolution_id_expected` (the one setting with a confirmed consumer — the E4.0 approval step via the payerResolutionIdentifier seam: org setting → Minted global fallback → generic). RLS: member SELECT own-org, ADMIN-only INSERT/UPDATE; no DELETE policy or grant (clearing = UPDATE to NULLs); revoke-then-grant floor. Nothing else moves here without a product-approved consumer |
| payer_network_targets | party | core | 1 | E1.5 group×payer×state attachment grain (distinct from the `org_payer_assignments` subscription layer — locked [stage-1b] split); `UNIQUE (group_id, payer_id, state)`, `status active                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | archived`(archive = removal semantic, never DELETE); RLS admin-write WITH CHECK requires same-org group + an`org_payer_assignments`row; E2.x case generation reads`status='active'`; 2026-07-20 re-scope added `payer_issued_id` (the group PIN, per state — board-edited, audited). **E6.8 `merge_payer`:** targets re-point to the survivor; on a (group, state) collision the survivor's row WINS (restored active if the loser's was active, inherits the loser's group PIN where it has none) and the loser's duplicate is ARCHIVED in place — the non-partial unique covers archived rows so the duplicate can never move, and the no-DELETE rule holds. **OPA-RETIRE (`20260810220000`):** INSERT/UPDATE WITH CHECK drops the assignment EXISTS (group-same-org + admin remain); attach no longer requires a subscription row |
| enrollment_facts | party | support | 1 | **E6.2 F6.2.5** — the migration-capture model at the case key's grain (provider, group, payer, state): "already enrolled with this payer UNDER THIS GROUP'S CONTRACT". Live facts count a payer row toward Active on the group fulfillment board (through the E6.0 `caseRollups` derivation) and suppress generation candidates (the E6.3 buffer math subtracts them); they NEVER create cases. Expiry is a FLIP (`expired_at`/`expired_by`), never a DELETE — partial `UNIQUE ... WHERE expired_at IS NULL` allows one LIVE fact per combination with expired history retained (re-record after expiry inserts a fresh row). `source` closed to `migration` today (widening is additive); optional `effective_date` backs "Active since". RLS: member SELECT, writer (admin\|specialist) INSERT/UPDATE with same-org provider+group WITH CHECKs; NO DELETE policy or grant. Migration `20260719150000` (repo-only; hosted apply = operator step). Capture UI is E6.4; E6.2 ships the model + board/candidate consumption; 2026-07-20 re-scope added `payer_issued_id` (the provider-level PIN, captured/edited on the enrollment — label from `payers.resolution_id_label`). **E6.8 `merge_payer`:** all facts re-point to the survivor; where both sides hold a LIVE fact at the same (provider, group, state) the loser's is first EXPIRED (the standard flip) and the survivor's inherits the loser's PIN where it has none |
| msos | party | support | 1 | |
| mso_routing_rules | party | support | 2 | no uniqueness/priority — overlapping rules are storable |
| parties | party | core | 1 | person/org contact entities; ORG-SCOPED since 2026-08-07 (D8 — `org_id NOT NULL`, cross-org reuse retired); split first/last + title/fax/extension |
| party_role_types | party | support | 1 | governed role reference list; all six roles active since 2026-08-07 |
| party_role_assignments | party | core | 2 | org-scoped party↔org role join; `is_default` = the ONE holder per (org, role) the contact tokens resolve |
| credential_cases | work | **core (center)** | 7 | `UNIQUE NULLS NOT DISTINCT (provider_id, group_id, payer_id, state)` since E2.1 (`20260713150000` — 4-part key for grouped rows, legacy NULL-group rows keep the 3-part rule); `generation_run_id uuid NULL → case_generation_runs` (E2.1 — NULL = manual/pre-E2.1); `state` format `^[A-Z]{2}$` enforced (E0.10). **E6.0 — `case_status text NOT NULL DEFAULT 'not_started'` CHECK 8-value (`20260719120000`, repo-only; hosted = operator step) is THE status**: the fixed canonical list (not_started→in_progress→submitted→in_review→action_required→approved\|denied\|not_pursuing, code-owned in `src/lib/caseStatus.ts`), backfilled deterministically from the (credentialing label × pipeline state) pair. Transitions ONLY via `set_case_status` (`20260719120100`, SECURITY INVOKER, atomic — history + evidence + terminal facts + audit) and the `20260719120200` evidence triggers (creation / first recorded work / extension submission, attributed system). `contract_executed_date date NULL` = the contracting replacement plain field (effective date reuses `confirmed_effective_date`). The two legacy machine columns — `credentialing_status_id` (E4.0-era internal track) and `payer_pipeline_state` 9-value (`20260715120000`) — are retained as READ-ONLY MIRRORS the RPC/triggers keep in lockstep for surviving legacy readers + the /api wire contract; the machines they backed are retired as user-facing. Approved-resolution columns `payer_individual_provider_id`/`payer_group_provider_id` (`20260715120500`) now written by `set_case_status` — **since E6.7 (`20260727120000` reissue) Approved requires EXACTLY the payer's expected IDs**: the individual ID when `payers.provider_id_expected` (legacy-pair fallback, NULL → true = the pre-E6.7 rule), the group ID when `payers.group_id_expected` (NULL → false); new named error `case_status_approved_needs_group_provider_id`; `advance_payer_pipeline` is DORMANT (kept per the additive rule, no callers). **E6.8 (`20260727150200`) reissued `set_case_status` again** (old 11-param signature DROPPED, 13-param created — the E4.2 publish-RPC no-overload precedent) with the per-ID **"Didn't receive" acknowledgments** `p_provider_id_missing_ack`/`p_group_id_missing_ack` (default false): at Approved each expected ID must be EITHER supplied OR explicitly acked missing — silence still raises the E6.7 errors; an acked ID stays NULL (Awaiting ID = expected + approved + NULL id, derived), the ack rides the audit `after` payload + a `Didn't receive: …` line in the history note, and back-fill stays the existing set-later paths. Merge (E6.8) re-points a loser payer's OPEN cases to the survivor (closed cases keep the merged loser id as history). **2026-07-22 (Cases redesign):** `case_number bigint NOT NULL` — a globally-sequential, immutable Case# (ONE sequence `credential_cases_case_number_seq` across ALL orgs, not per-org), backfilled in global `created_at` order (base 1001), drawn by a column DEFAULT on every insert path (the `create_case_with_tasks` explicit column list is unchanged — no RPC change); global `UNIQUE (case_number)`, immutability enforced by a BEFORE UPDATE trigger; migration `20260722120000`, repo + hosted (applied via MCP) |
| case_generation_exclusions | work | support | 1 | E2.0 generation-input side-table to the `credential_cases` center: persistent reasoned per-key exclusions at the 4-part grain (provider, group, payer, state); partial `UNIQUE ... WHERE status='active'`; restore = void (`status`/`voided_by`/`voided_at`), never DELETE — no DELETE grant; admin-only writes ([r4-review] Q2), member SELECT |
| case_generation_runs | work | support | 2 | E2.1 batch record (who/when/counts) for a confirmed generation run; `credential_cases.generation_run_id` FKs it. IMMUTABLE by omission — no UPDATE/DELETE policy or grant; member SELECT, writer INSERT. Stored counts = the confirm-time plan; E2.4's disposition child rows supersede them at read time (its TE-1); **E4.2** `release_scope jsonb` (TE-14 — release scope the run used, NULL = full run) |
| contracts | work | core | 2 | `group_id`/`payer_id` NOT-NULL + `state` format enforced via validated CHECKs (E0.10) — the one-contract-per-(group,payer,state) invariant is now real; `contracting_status_id` still nullable. **E6.0:** the contracting status machine is retired as user-facing (no status-setting UI; execution/effective dates live on the case — `contract_executed_date`/`confirmed_effective_date`, set at Approved); the column stays readable for derived checks until E6.2 re-expresses fulfillment |
| tasks | work | core | 5 | owner CHECK enforced (E0.10): a task must carry `case_id` or `provider_id`; `sop_template_id`/`sop_version` stamp columns (E1.7b — nullable, both-or-neither CHECK, composite FK to `sop_template_versions`; written by E2.2 generation); **E4.2** `execution_type` (TE-12 — nullable CHECK `manual                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | extension_fill                                                                                                                                                                      | auto_verify | document_attach`, null ⇒ manual; stamped at generation, R6 renders only); **E4.2 SOP hardening** `sop_resolution_tier`(nullable CHECK`organization\|global_payer\|generic_fallback`, migration `20260716120000`) — stamped from the deterministic `pickTemplate` tier so a manual case (no generation run) stays directly tier-reportable without reconstructing it from mutable template ownership |
| sop_templates | work | support | 1 | matching precedence lives in `src/lib/pickTemplate.ts` — **3M Slice 3 / D3.3-G**: state specificity (exact two-letter > `All` sentinel) → group specificity (exact group > any-group) → ownership (org > global payer) → generic fallback → null (order-independent; another group/payer never resolves; `state='All'` is the All-states wildcard, not null Any-state); `current_version` head pointer (E1.7b); a payerless global row is the seeded generic fallback, visible to all orgs; **E4.2 SOP hardening** additive partial unique index `uq_sop_templates_active_org_match (org_id, payer_id, state, group_id) NULLS NOT DISTINCT WHERE org_id IS NOT NULL AND payer_id IS NOT NULL AND state IS NOT NULL AND archived = false` (migration `20260716120000`) — one active org SOP per supported grain; **specialty is preserved legacy/non-routing metadata, NOT a runtime match key**; **E4.2** `required_profile_attributes jsonb` (TE-13 governed gate keys, head working copy) and per-task `executionType` inside `task_definitions` (TE-12). **E6.7 F6.7.2 (`20260727120100` reissues of `author_global_sop` + `publish_sop_template_version`): the generic fallback's blanket `fallback_sop_locked` rejection for authenticated callers is REMOVED** — its content publishes like any global SOP; the structural guards stay (cannot be archived, cannot be given a payer/state/group — same error name; every OTHER active global SOP still requires payer+state, so it remains the only payerless global row by construction). **3M Slice 6 D6.5 Option A (`20260809120100_slice6_global_sop_read_without_assignment`, repo-only — hosted apply = operator step): `sop_templates_select` widened so ALL global rows (`org_id IS NULL`) are readable by any authenticated member**, replacing the assignment-gated global disjunct and subsuming the payerless-fallback disjunct (always a special case of it). Without this, a SOP authored for a payer created with `p_assign_to_org = false` was written successfully and then invisible to its author (`author_global_sop` never required an assignment; the LIST read did) — write-then-vanish. This mirrors `portals_select_org`, which has been unconditional for global rows since E6.5, and is strictly narrower than global SOP AUTHORING, which E6.5 already opened to any authenticated caller (TD-42). Resolution is unaffected for unadopted payers: `pickTemplate` matches payer exactly and a case only exists for an adopted payer, so newly visible rows for other payers can never win a match (pinned in `pickTemplate.test.ts`). SELECT only — every write policy is unchanged, (`payers_select` WAS later widened by OPA-RETIRE — listPayers is the full catalog; "my network" is target-derived in the app) |
| sop_template_versions | work | support | 1 | E1.7b Model A immutable version rows — INSERT-only via `publish_sop_template_version` RPC / creation trigger / backfill; no `org_id` (tenancy via parent EXISTS); `UNIQUE (template_id, version)` is the `tasks` stamp FK target; **E4.2** `required_profile_attributes jsonb` snapshotted per version by the publish RPC (TE-13). **3M Slice 6 D6.5:** `sop_template_versions_select` restates the widened parent disjunct (`t.org_id IS NULL`), or version history / SOP provenance would render blank for a global template its reader can now see |
| status_configs | work | core | 3 | `UNIQUE (org_id, track, label)` enforced (E0.10). **E6.0:** no longer a user-facing machine anywhere — the Admin › Statuses page is a redirect and no per-org status CRUD exists; rows are retained as the legacy-mirror lookup (`set_case_status` resolves the credentialing mirror row by canonical label) and for the retained read-only ledgers |
| import_runs | work | support | 1 + RPC writes | E3.0 TE-2 bulk-roster-import run header — the durable async-scan progress record (state `uploading→scanning→ready_for_review/committed/failed/cancelled`, staged/error counts, `error_report` jsonb). A WORKING table, not a ledger: UPDATE allowed for state/count progress; member SELECT, ADMIN-only INSERT/UPDATE, no DELETE grant. E3.1 adds `committed_at`/`created_provider_ids`/`updated_provider_ids` and the transactional `commit_import_run` SECURITY DEFINER RPC that flips `ready_for_review`→`committed` (idempotent via a state guard), does ALL live-table writes in one tx, and purges the run's `import_rows`. E3.3 adds `entity_kind` (`provider_group\|facility\|provider\|combined`, default `combined` so in-flight E3.0 runs stay valid) — the additive discriminator that lets the one staging machine serve the three per-section uploads; the provider path uses `commit_import_run`, the group/facility paths fan out to `createProviderGroup`/`createFacility` (migration `20260714120000_import_runs_entity_kind.sql`). **E6.2** widens `entity_kind` with `payer_attach` (`20260719150100`, repo-only): the group×payer attach CSV rides the same machine — scan-time eligibility via the descriptor's org-context scan, commit fans out to `attachGroupPayer` (idempotent skip-on-match; archived targets RESTORE; org enablement implicit) |
| import_rows | work | support | 1 + RPC writes | E3.0 TE-2 staging grain — one row per parsed source line (`UNIQUE (run_id, line)` = the idempotent resume key); `raw`/`mapped` jsonb hold PII under org RLS, and a full SSN is rejected at scan and NEVER lands here (TE-6). Written via the batched `stage_import_rows` SECURITY DEFINER RPC (admin-checked, recomputes run counts); member SELECT, ADMIN INSERT/DELETE — rows are PURGED on commit (E3.1) or cancel (TE-7). Never feeds live provider/group/facility tables in E3.0 |
| case_generation_run_rows | ledger | support | 2 | E2.4 TE-1 immutable per-candidate disposition record (one row per 4-part key per run, `UNIQUE (run_id, provider, group, payer, state)`); INSERT-only — no UPDATE/DELETE policy or grant; member SELECT, writer INSERT with same-org run/provider/group WITH CHECKs; `case_id` links created AND blocking cases (SET NULL), `exclusion_id` SET NULL + `reason` snapshot so links degrade, never dangle; counts derive from these rows at read time, superseding the run's stored plan; **E4.2 SOP hardening** additive `sop_template_id`/`sop_version`/`sop_resolution_tier` (nullable CHECK `organization\|global_payer\|generic_fallback`, migration `20260716120000`) — a confirm-time SNAPSHOT (plain columns, no FK, like `reason`) recording the resolution provenance of every `created` row so generic-fallback usage is countable by run/payer/state/group/org; **E6.3** disposition CHECK widened (`20260719160000`) with `skipped` (skip-for-now — stays a candidate) and `enrolled` (covered by a live enrollment fact), both reason-required, so a confirm's ledger accounts for EVERY grid candidate |
| touches | ledger | core | 7 | the touchlog; reference-standard table shape. E4.1 (migration `20260715130000_structured_touches.sql`) widens `touch_type` to the seven fixed types {call, email, portal, fax, caqh_update, provider_outreach, internal_sync} ∪ legacy `mail`, adds the optional disposition outcomes {successful, attempted, no_response, error, other}, loosens the touchpoint-shape check so `outcome` may be NULL on a typed touch, and adds additive columns `clears_follow_up` (F4.1.2 carry-forward — the only way to end a follow-up), `recipient_name`/`recipient_contact` (F4.1.5), and `corrects_touch_id` (self-FK; corrections are appends). Still append-only (no UPDATE/DELETE) |
| status_history | ledger | core | 1 + RPC writes | append-only; XOR case/contract enforced |
| payer_pipeline_history | ledger | core | 1 + RPC writes | E4.0 TE-2 append-only external-pipeline transition log (`20260715120300`); `from_state`/`to_state` same 9-value CHECK domain as `credential_cases.payer_pipeline_state`; `reason_code_id → denial_reason_codes` (SET NULL); `is_correction` + `justification` (CHECK justification present when correction; also stores the Denied-"Other" context); member SELECT (all roles read the visible timeline), writer INSERT with same-org case WITH CHECK, NO UPDATE/DELETE policy or grant; FK-cover index on `case_id`. Written ONLY by `advance_payer_pipeline`. **E6.0:** retained READ-ONLY (rendered as a legacy panel on case detail); nothing writes it anymore — the unified trail is `case_status_history` |
| case_status_history | ledger | core | 1 + RPC/trigger writes | **E6.0** append-only unified-status transition log (`20260719120000`, repo-only; hosted = operator step): `from_status`/`to_status` in the 8-value canonical domain; `actor_kind system\|user` (F6.0.2 attribution); `evidence_touch_id → touches` (SET NULL — the F6.0.3 Add-touch evidence link); `reason_code_id → denial_reason_codes` (SET NULL — the required denial reason); `is_correction` + `note` (CHECK note present when correction; also the Not Pursuing note / Denied-"Other" context); member SELECT (all roles), writer INSERT with same-org case WITH CHECK, NO UPDATE/DELETE policy or grant; FK-cover indexes. Written ONLY by `set_case_status`, the `_apply_case_status_auto` triggers, `create_case_with_tasks`, and the one-time migration backfill |
| denial_reason_codes | party | support | 1 | E4.0 TE-4 governed denial/return reason vocabulary (`20260715120200`); `org_id NULL` = global default (six seeded), non-NULL = org-added; **E4.2 F4.2.3 CRUD now built** (`denialReasonCodes.ts` — add/deactivate org codes; global defaults non-deletable and non-deactivatable by orgs); `UNIQUE NULLS NOT DISTINCT (org_id, code)`; member SELECT (global + own-org), admin INSERT/UPDATE; deactivate (`active=false`), never delete, so history references never dangle |
| communication_event | ledger | support | 2 | batch-call parent |
| audit_log | ledger | core | 2 + RPC writes | append-only, immutable |
| notes | ledger | frozen-mirror (case/task) / support (provider) | 1 | case/task rows migrated to `touches` (2026-07-07); still the live store for provider notes |
| notes_pre_touchlog_backup | ledger | dormant | 0 | migration backup |
| launches | work | dead | 0 | replaced by facilities location track; only inbound ref is the dead `providers.launch_id` |
| provider_documents | work | core | 4 + server routes | **ACTIVATED by E4.5** (`20260717150000`) as immutable document-version metadata: additive `document_family_id` (stable lineage; legacy rows backfilled as their own single-row families), `version_number` (positive CHECK), `supersedes_document_id` (self-FK + partial unique = ONE successor per superseded row — history is a chain, never a fork); `UNIQUE (org_id, document_family_id, version_number)` is the idempotent-finalize key. "Current" is DERIVED (the family row with no successor) — replacement INSERTS, never updates/deletes: client grants cut to SELECT+INSERT (broad baseline anon/authenticated DML revoked), delete policy dropped. `doc_type` CHECK widened additively with `cms_460`/`cv`; NOT VALID CHECK `state_license\|dea\|coi` require `expiration_date` (historical rows grandfathered — operator may VALIDATE after audit). Objects live in the private `provider-documents` Storage bucket (`20260717150100`, org-bound path contract `org/{orgId}/{provider\|group}/{ownerId}/{familyId}/{version}/{file}`, storage.objects policies validate the path org vs membership; no public URLs ever). Signed upload-intent/finalize/download are guarded /api routes (`src/server/documentRoutes.ts` → `src/services/documentStorage.ts`, audited, no-store, gate assertions 17/17b); metadata list reads stay browser-RLS (`src/services/documents.ts`). Legacy case-linked `filled_form` rows + the baseline owner CHECK preserved |
| group_insurance_policies | party | support | 4 | template for planned `provider_insurance_policies`. **`coverage_level` (`primary\|secondary`, default `primary`, CHECK; `20260729120000`)** makes the group's primary policy a stored fact instead of a newest-end-date heuristic, with secondary coverage tracked beside it — partial unique `uq_group_insurance_policies_one_primary (group_id, insurance_type) WHERE coverage_level='primary'`. The ONE capture surface is `components/groups/InsurancePanel.tsx` (onboarding wizard group section + the standalone group hub); it is deliberately NOT fields on the group metadata form, which can hold only one policy. `providerProfile.pickPolicy` prefers the primary row |
| user_table_prefs | config | support | 2 | also backs extension view prefs |
| public_rpc_attempts | config | support | 0 + anon RPC writes | rate-limit attempt counter for the anon RPCs (E0.7/E0.8); written inside SECURITY DEFINER RPCs, no direct `src/` reads |
| portal_field_maps | extension | core (ext) | 1 + server routes | `org_id` NULL = SHARED row (the trained-form library, E6.9 D12); non-null = org row. **E6.9 field registry (`20260807160000`, repo-only):** additive `display_label` / `section` / `sort_order` — the ADMIN's name, grouping and position, paired with the existing `field_label` / `form_section` which stay the payer's RAW captured text, so a re-capture refreshes what the page says without clobbering a rename (D6/D7). Ordering and uniqueness are **per tier**: partial uniques `uq_portal_field_maps_shared_selector (portal_key, selector) WHERE org_id IS NULL` and `uq_portal_field_maps_org_selector (org_id, portal_key, selector) WHERE org_id IS NOT NULL` finally back the `(portal_key, selector)` idempotency that was a bare read-then-insert (concurrent captures could duplicate); `sort_order` backfills from `created_at` partitioned by `(org_id, portal_key, page_step)` so the shared library never interleaves with an org's rows. Shared rows fail browser RLS for INSERT/UPDATE, so every shared write goes through the `20260807160100` SECURITY DEFINER RPCs — `propose_shared_field_map` (ON CONFLICT DO NOTHING + re-read, so re-capture preserves decisions), `train_global_field_map` (REISSUED: now accepts `source='hardcoded'` + a non-empty literal, and repropose clears `token` AND `hardcoded_value`), `update_shared_field_registry` (batch display_label/section/sort_order; present-null clears, absent leaves). Ungated for any signed-in user (D11); no audit rows for shared writes (`audit_log.org_id` is NOT NULL) — `updated_at` is the trail (D14) |
| portals | extension | support | 1 | owns the `portal_key` natural key shared with `portal_field_maps` / `fill_sessions` (no FK). **E6.5 global tier (`20260719170000`, repo-only):** `org_id` NULLABLE — NULL = a GLOBAL registry row (partial unique `uq_portals_global_key (portal_key) WHERE org_id IS NULL`; SELECT gains the global disjunct; global writes ONLY via the `upsert_global_portal`/`set_global_portal_flags` SECURITY DEFINER RPCs — interim all-authenticated posture, R7 hardens); additive `proven_at` = the F6.5.3 mock-dry-run pass stamp (cleared with verification on a form-URL change). **3M Slice 6 D6.4 (Slice 4 audit F24, no migration — a read-path filter):** `/api` registry reads **and** browser `listPortals` (panel `usePortals` pickers) drop GLOBAL rows that are not workable forms — `payer_id` null, or a payer that is retired / merged / archived (`src/lib/portalVisibility.ts`, applied in `listSharedPortals` for Train, the global leg of `listPortalsForApi` for Work-case recognition, and `listPortals` for webapp SOP/case portal selects). Own-org rows are untouched. The predicate FAILS CLOSED on a missing payer embed: a hidden ghost costs a re-register, a listed one costs a trainer real work on a form that can never be used |
| provider_field_verifications | extension | support | 2 | **Workbench S6.1** (`20260728120000`) per-field verification stamps — ONE narrow table keyed `(provider_id, field_key)` rather than N `*_verified_at` columns on `providers` (a new verifiable field costs a row, never a migration). `field_key` is the BARE catalog token key (`provider.npi`) — the identifier the profile endpoint, `portal_field_maps` and the quick-card catalog already share — and is deliberately NOT an FK (the token catalog derives from `information_schema`). `unique (provider_id, field_key)` keeps ONE current stamp per field; re-verification updates in place and history lives in `audit_log`. `verified_source` CHECK `manual\|caqh\|extension`. Freshness window is `CAQH_CURRENT_DAYS` via `src/lib/fieldVerification.ts`. **`20260728160041` fixes a real cross-tenant hole:** the org check was written `exists (select 1 from providers p where p.id = provider_id and p.org_id = org_id)`, where the unqualified `org_id` binds to the INNER scope — Postgres stored it as `p.org_id = p.org_id`, a tautology, and a live probe confirmed an admin in org A could stamp org B's provider. The scalar form `(select p.org_id from providers p where p.id = provider_id) = org_id` compares in the outer scope and cannot be captured |
| fill_sessions | extension | support | 2 | **E4.2 F4.2.7** `is_test boolean DEFAULT false` (dry-run marker excluded from firstPassRate + all metric readers); `case_id` relaxed to NULLABLE so a test fill needs no case; `fields_skipped` jsonb typed to structured `{selector,label,reason}[]` (echo unchanged) |
| next_best_action_configs | support | support | 0 | **E4.2 F4.2.5/TE-7** org-level My Cases queue ranking (`org_id` PK, `ranking jsonb` = validated `{order:[...]}` of the four fixed keys, `updated_by`/`updated_at`); org-member SELECT, admin INSERT/UPDATE/DELETE (reset = row delete); **DORMANT since E6.6 F6.6.6** — the ranking editor and BOTH reads (browser + `/api/next-best-action`) are retired; the queue runs the fixed shipped order (`src/lib/nextBestActions.ts`). Table + policies kept per the additive rule; rows inert (TD-44) |
| sop_template_drafts | work | support | 0 | **E4.2 F4.2.1** SOP wizard save-as-draft WIP (`org_id`, optional `template_id → sop_templates`, `payload jsonb`, `updated_by`, timestamps); admin-member RLS; NEVER resolved by `pickTemplate` or readiness; deleted on successful publish |
| field_dictionary | extension | support | 1 | |

## Design rules (defended by this register)

1. **Grain rule.** Every table has exactly one grain (case = provider×group×
   payer×state; contract = group×payer×state; license = provider×state). A new
   field
   goes on the table whose grain matches how the field varies. A field that
   varies by state, purpose, or payer is a child row keyed by that dimension —
   never a new column on a grain-less master row. (The `provider_groups`
   billing/correspondence/credentialing column blocks are the cautionary tale.)
2. **M:N rule.** Any relationship that could plausibly become many-to-many gets
   a join table from day one. `memberships`, `org_payer_assignments`, and
   `provider_facility_assignments` are the house pattern. `providers.group_id`
   (single-group assumption, now known false) is the cautionary tale.
3. **Ledger rule.** History lives in append-only event tables, never in master
   rows. Latest-wins columns (e.g. `credential_cases.payer_reference_id`) are
   acceptable only when per-event history explicitly lives in the touchlog.
4. **Supersede, don't mutate.** Per the additive migration rule, a replaced
   structure becomes a frozen mirror with its replacement named here; when the
   redesigned UI leaves a mirror with no readers, it is re-statused dormant.
