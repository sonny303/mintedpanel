# Populated staging alignment plan

Status: **PLANNED — no alignment SQL authored or applied by this review**.
Requirement: AUD-02 in [the release packet](README.md). Execution belongs to PR #396
and its reviewed follow-up; production remains approval-held.

This plan targets only Supabase project `vmznysvietfaddakkegt`. It must never run
against production `fkvuhfsqcmujywzgczmc`. It uses existing staging data, not a
production data copy. The production packet remains exactly the two function
migrations in [manifest.json](manifest.json); none of the backfills below belongs
in that packet. UAT seed execution is a separate, later staging-only operation.

## Recorded baseline and target

| Evidence                                        | Recorded result                                                                                                                                                                  | Interpretation                                                                                                                                                                                 |
| ----------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Repository inspected                            | `43b59cfa95c84a929c452cc097453c802bbef50b` plus the current recovery/test working changes                                                                                        | Re-pin the final source SHA and individual source-file checksums before authoring the delta.                                                                                                   |
| Staging catalog                                 | 53 public tables, 647 columns, 34 function signatures, 142 policies                                                                                                              | Catalog snapshot, not a complete ownership/grant/default/index inventory.                                                                                                                      |
| Required missing app tables                     | `case_facilities`, `case_status_history`, `enrollment_facts`, `payer_contacts`, `payer_forms`, `provider_field_verifications`, `provider_ssn_intake_links`, `provider_ssn_vault` | Eight additions, taking the recorded public-table count to 61. Existing legacy tables remain.                                                                                                  |
| Column target                                   | 119 missing app columns                                                                                                                                                          | Preserving the 12 staging-only columns gives **766** columns, not production's 813. Production's 59 operator-backup columns are excluded. Counts are a cross-check, never the acceptance test. |
| Existing cases                                  | 65; 61 have an explicit facility, 4 do not                                                                                                                                       | Preserve every case ID and its selected facility; never substitute a provider's primary facility.                                                                                              |
| Status preflight supplied by coordinating audit | Unknown legacy label/pipeline counts: 0                                                                                                                                          | Refresh inside the locked rehearsal/apply boundary. Do not rely on an earlier count after concurrent writes.                                                                                   |
| Storage/Vault                                   | No staging buckets; `ssn_vault_key` absent                                                                                                                                       | Create only independently configured staging resources. Copy no production objects or secrets.                                                                                                 |
| Already-applied repairs                         | Staging CSV parity; `delete_case`; INSERT-only generated-case validator; case FK `ON DELETE SET NULL`; obsolete created-case CHECK absent                                        | Preserve exact definitions, grants and behavior. Two ledger entries do not mean the other historical files are unapplied.                                                                      |

Evidence inputs are the read-only `schema-drift.md`, `staging-catalog.json` and
`vmznysvietfaddakkegt-constraints.json` from the September 22 audit, the coordinating
agent's aggregate data preflight, and repository SQL inspected September 23 UTC.
No new hosted query or write was made for this document.

## Gate 0 — qualify the populated baseline first

1. Finish the fresh encrypted staging capture, temporary-role cleanup, isolated
   restore and integrity checks. Record capture/restore receipt IDs, exact source
   cluster identity, schema hashes, row counts and encrypted artifact hashes.
   A capture or partial restore alone does not satisfy AUD-01.
2. Prepare two independent isolated restores: an untouched comparison copy and a
   delta rehearsal copy. Retain Auth users/identities and relationships; never
   manufacture parity with `db reset`, seed replacement or production restore.
3. Inventory defaults, indexes, sequence values, constraints/validation state,
   owner/search-path/ACL/SECURITY DEFINER attributes, extension versions, Auth
   schema dependencies, Storage policies and migration ledger. Normalize function
   definitions only for comparison; explain body, configuration and grant changes
   separately. Hash differences alone are not proof of missing behavior.
4. Freeze an object-level manifest: each changed object, expected prestate,
   desired poststate, exact repository source, row predicate, expected affected
   count and allowed unchanged columns. Capture before-images privately; report
   aggregate counts/hashes only. Add no person data or secrets to this packet.
5. Run every prerequisite below on the restored baseline. Stop a slice on an
   ambiguity; do not guess a contact's organization, delete conflicting rows,
   invent identifiers/dates, or choose a different facility to make a constraint pass.

## Ordered source map for the new staging-only delta

The filenames below are **source references, not a shell execution list**. Author
new additive reconciliation migrations only after Gate 0 passes. Existing files
remain immutable. Compose final object definitions, avoiding intermediate versions
that temporarily reopen authorization or replace newer behavior. Each numbered
slice is a bounded transaction with prestate assertions, lock/statement timeouts,
poststate assertions and a recorded checksum; dependent slices execute serially.

| Order                                               | Source files under `supabase/migrations/`                                                                                                                                                                                                                                                                                                                                    | Exact scope and dependencies                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1 — canonical cases                                 | `20260719120000_e60_unified_case_status.sql`; `20260719120200_e60_auto_status_transitions.sql`; final RPC from `20260727150200_e68_case_status_missing_ack.sql`                                                                                                                                                                                                              | Add `case_status`, `contract_executed_date`, history table/indexes/RLS; perform B1–B3 below. Install final transition RPC after payer ID-expectation columns from slice 2 exist; activate the three auto-transition triggers only after backfill. Earlier RPC versions `20260719120100` and `20260727120000` are comparison sources, not required intermediate installs.                                                                                                                   |
| 2 — payer schema and controlled writes              | `20260719170000_e65_global_authoring.sql` (payer `delegation_note` only); `20260727120000_e67_payer_manual_setup.sql`; `20260718120000_payers_org_write_lockdown.sql`; `20260727150000_e68_payer_archive.sql`; `20260727150100_e68_merge_payer.sql`; final `create_payer`/policies from `20260810220000_opa_retire_assignment_gate.sql`                                      | Add ID-expectation/provenance/archive columns and normalization helpers. B7 maps only absent values. Validate normalized global-name uniqueness first. Install final manual create/update/archive/reactivate/merge definitions with their grants. Retain all payer rows and dormant `org_payer_assignments`. Remove direct payer INSERT/UPDATE paths as specified by the governed RPC design, in the same slice that supplies the replacement RPCs.                                        |
| 3 — enrollments and generation constraints          | `20260719150000_e62_enrollment_facts.sql`; `20260720230000_payer_issued_enrollment_ids.sql`; `20260719150100_e62_import_payer_attach_kind.sql`; `20260719160000_e63_run_row_dispositions.sql`; `20260719190000_grant_floor_hardening.sql`                                                                                                                                    | Create empty `enrollment_facts`, including nullable `payer_issued_id`; add nullable target-level payer ID. Widen import kind and generation disposition/reason checks. Floor ledger grants explicitly. **Do not derive enrollment-fact rows from approved cases**: the application already derives those case enrollments. Do not replace `commit_import_run`.                                                                                                                             |
| 4 — case identity and facilities                    | `20260722120000_case_number.sql`; `20260827120000_e11_case_facilities.sql`                                                                                                                                                                                                                                                                                                   | B4–B5 assign missing case numbers and create primary facility joins. Preserve `credential_cases.facility_id`, including explicitly selected secondary facilities. Install sequence grants and immutability trigger after the backfill. Rehearse the already-present `delete_case` after all dependencies exist, without replacing it.                                                                                                                                                      |
| 5 — contact tenancy, defaults and RPC compatibility | `20260721120000_party_role_label_terminology.sql`; selected schema/backfill from `20260807130000_people_contact_roles.sql`; `20260807130100_people_contact_role_rpcs.sql`; `20260807150937_harden_party_role_tenant_integrity.sql`                                                                                                                                           | B8–B9 resolve contact ownership and defaults. All contact-writing RPCs, org-scoped RLS, composite FK and immutable-org trigger must land atomically with NOT NULL. **Exclude the historical orphan DELETE**. The later RPC source already carries optional-sales-rep behavior; do not replay the cleanup DML in `20260807120000_org_intake_no_default_sales_rep.sql`. Preserve original party and role-assignment IDs.                                                                     |
| 6 — document metadata and storage                   | `20260717150000_e45_provider_documents_activation.sql`; final kind check from `20260824153000_provider_documents_cp575_kind.sql`; `20260717150100_e45_document_storage_bucket.sql`; `20260824170000_payer_forms.sql`; final form FK/trigger and RPC from `20260903210000_delete_org_sop_template.sql`                                                                        | B6 adds version metadata to existing documents. Create private `provider-documents` and `payer-forms` buckets/policies, without object copies. Create empty `payer_forms` directly with final nullable template FK `ON DELETE SET NULL` and final retire/detach trigger. No delete RPC is called during migration. `form-templates` has no repository bucket migration found: confirm current consumer need/configuration before adding it; do not copy production settings by assumption. |
| 7 — field verification and coverage                 | `20260728120000_s61_provider_field_verifications.sql`; final policies from `20260728160041_s61_fix_provider_org_match_ambiguity.sql`; `20260729120000_group_insurance_coverage_level.sql`                                                                                                                                                                                    | Create empty `provider_field_verifications` using corrected same-org predicates from the outset; never expose the known tautological policy. B10 adds insurance coverage level only after checking that the one-primary invariant can hold without arbitrary reclassification.                                                                                                                                                                                                             |
| 8 — global portals, SOPs and registry schema        | `20260719170000_e65_global_authoring.sql`; final publish RPC from `20260727120100_e67_fallback_template_unlock.sql`; `20260809120100_slice6_global_sop_read_without_assignment.sql`; `20260807160000_e69_field_registry.sql`; `20260812140000_sop_template_multi_state.sql`; `20260813120000_e610_control_options.sql`; `20260814180000_author_global_sop_returns_jsonb.sql` | Allow global portal tier without rewriting existing org ownership; add `proven_at`, registry presentation/options, SOP `states`; B11–B12 backfill known values. Install final `author_global_sop` JSONB-returning signature directly; preserve scalar `state`. Replace old SOP uniqueness mechanism only with overlap preflight and final trigger ready in the same transaction. No authentication-policy expansion beyond the reviewed source design.                                     |
| 9 — registry RPCs and existing-template data        | Final `propose_shared_field_map` from `20260825120000_e611_pdf_field_maps.sql`; final `train_global_field_map` from `20260813120000_e610_control_options.sql`; `update_shared_field_registry` from `20260807160100_e69_shared_registry_rpcs.sql`; `20260807170000_e69_datafields_to_registry.sql`                                                                            | Install only final signatures so PostgREST has no ambiguous overload. The intermediate `20260810143000` proposal refresh is already incorporated into the final definition. B13 migrates existing online-form data fields only after tenant/shared classification and version preflight; postpone that DML if it would publish org-private definitions to the shared tier.                                                                                                                 |
| 10 — remaining read policy and profile columns      | `20260812130000_payer_contacts_select_opa_retire.sql`; `20260816120000_profile_name_split_and_title.sql`; contact table/RPC source `20260727120200_e67_payer_contacts.sql`                                                                                                                                                                                                   | Create empty `payer_contacts` before its final read policy. Add nullable profile first/last/title with **no heuristic backfill** and keep `full_name` unchanged. This slice's contact-table creation may execute earlier with payer slice 2; final policy needs only that table plus existing membership helpers.                                                                                                                                                                          |
| 11 — vault, immediately repaired                    | `20260717120000_ssn_vault.sql`; `20260717120100_ssn_intake_links.sql`; `20260718020000_ssn_vault_key_via_supabase_vault.sql`; final operator functions from `20260918172142_p01_vault_authorization.sql`                                                                                                                                                                     | Verify `extensions.pgcrypto`, Vault access and existing throttle/audit dependencies. Create two empty vault/intake tables and private helpers, with final NULL-safe P01 operator functions in the **same transaction before grants become usable**. Provision a new independent staging secret via the protected operator path; never copy/rotate production's key. No SSN backfill from `ssn_last4`, no synthetic SSNs during schema apply.                                               |
| 12 — final compatibility/permission review          | Current restored objects, latest repository definitions, `20260919033009_provider_csv_add_provider_parity.sql`, `20260921220000_fix_case_generation_run_rows_created_case_check.sql`                                                                                                                                                                                         | Assert the already-applied CSV/delete/ledger behavior remains intact. Compare every remaining function/config/grant difference from the catalog individually; preserve legitimate provider-managed differences. Do not blindly replace old functions such as `create_case_with_tasks`, `user_role`, `user_org_ids`, `handle_new_user`, rate-limit helpers or `rls_auto_enable` from a baseline dump.                                                                                       |

Slices 1 and 2 have a small dependency cycle only in function installation: add
both sets of columns first, finish the status backfill, then install the final
status RPC/triggers. No app request may observe half of that compatibility group.
Likewise slice 10's payer-contact table precedes its final policy. The source map
allows independent object preparation; it does not authorize out-of-order exposure.

## Exact data transformations and preservation checks

| ID  | Transformation                                                                                                                                                                                                                                                                                                                                                                          | Preconditions and postconditions                                                                                                                                                                                                                                                                                                                                                                                    |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B1  | Populate `case_status` using E6.0's precedence: progressed pipeline wins; otherwise legacy credentialing label. Pipeline `assigned`/`drafting` → `in_progress`; `oon` → `not_pursuing`; other progressed values map directly. Legacy `Waiting on Provider` → `in_progress`, `In-Network` → `approved`, `OON`/`Not Required` → `not_pursuing`; recognized `Not Started` → `not_started`. | Refresh unknown-value count = 0 before apply. Recorded expected counts: submitted 32, approved 14, not_started 12, in_progress 3, not_pursuing 2, in_review 1, denied 1, action_required 0; total 65. Preserve IDs and both legacy fields. If canonical status already exists after a partial operation, do not overwrite later transitions from an older mapping.                                                  |
| B2  | Fill a missing `confirmed_effective_date` only for an approved case from a contract with identical `org_id`, `group_id`, `payer_id`, `state` and a non-null effective date.                                                                                                                                                                                                             | Count eligible rows and prove at most one matching contract per case first. Preserve every non-null case date. Do not synthesize `contract_executed_date` or substitute an org-wide/payer-only date. Record exact affected count; presently unmeasured.                                                                                                                                                             |
| B3  | Append one initial `case_status_history` row for each case without any unified history, `from_status=NULL`, `actor_kind='system'`, E6.0 migration note, current mapped status.                                                                                                                                                                                                          | Expected 65 if refreshed history remains absent; no rewriting/deleting old `status_history` or `payer_pipeline_history`, no invented user/touch evidence. Retry adds zero duplicate history rows.                                                                                                                                                                                                                   |
| B4  | Assign numbers only to unnumbered cases in stable global `(created_at,id)` order, starting above `max(1000, existing maximum, already-issued sequence bound)`. Set default and advance sequence safely; make numbers unique/NOT NULL and immutable.                                                                                                                                     | With the recorded all-unnumbered 65 rows, expected values are 1001–1065. The old file's unconditional 1000-based numbering of NULL rows is unsuitable for a partly numbered database. Preserve all existing numbers; never lower the live sequence or renumber rows. Note that sequence changes are not transactionally rolled back; gaps are acceptable, reuse is not.                                             |
| B5  | For each case with an explicit facility, add its same facility to `case_facilities` as primary, `created_by=NULL`, only when no matching join exists.                                                                                                                                                                                                                                   | Expected 61 inserted joins on the recorded empty target. Validate facility exists and matches case org; separately flag provider/group eligibility issues. Preserve four NULL facilities without guessing. On a partially populated target, verify any existing primary matches the case mirror; do not silently accept a conflicting primary or rely solely on `ON CONFLICT DO NOTHING`.                           |
| B6  | Existing `provider_documents`: one new unique family per legacy row, version 1, no predecessor; preserve any already-populated family/version values.                                                                                                                                                                                                                                   | Preserve document IDs, owner/case links, paths, filenames and dates. Reuse valid existing version chains; validate no forks/duplicate family versions. Preserve historical missing-expiration rows with the source's NOT VALID check; do not fabricate expiration dates or validate that check without a separate data audit.                                                                                       |
| B7  | On payers with both new provider-ID fields absent, copy the legacy resolution-ID label/expectation pair; set absent `source='sync'`.                                                                                                                                                                                                                                                    | No group-ID inference, invented creator/timestamp, name edits, archive/merge calls or catalog deletions. Validate domain values and normalized-name uniqueness first. Preserve all non-null new values.                                                                                                                                                                                                             |
| B8  | Contact names: split legacy `parties.name` with the repository's last-space helpers only when both first/last fields are NULL; retain `name`. Ownership: derive from assignments only when all assignments agree on one org; otherwise from creator's sole membership when there are no assignments.                                                                                    | Preflight cross-org assignments and unresolved creators. The historical earliest-assignment choice is insufficient for a shared party. **Stop** on ambiguous/unresolved parties; preserve them, do not delete or silently choose an org. NOT NULL and tenant FK require zero unresolved/mismatch rows. A necessary split/relink needs a separately reviewed mapping preserving relationships.                       |
| B9  | For each `(org_id, role_key)` with no default, mark the earliest `(created_at,id)` role assignment as default. Apply recorded role-label terminology and activate only the three reserved roles.                                                                                                                                                                                        | Preserve an already-established default; fail on duplicates instead of resetting them. Keep assignment IDs/scopes/party links. Role-label changes are closed reference data, not UAT fixtures.                                                                                                                                                                                                                      |
| B10 | Set new `coverage_level='primary'` for existing group insurance rows only where one row per `(group_id,insurance_type)` makes that truthful.                                                                                                                                                                                                                                            | Multiple legacy policies in a grain require an explicit primary decision; no newest-date heuristic or deletion. Preserve all policy rows and values; enforce one primary after resolution.                                                                                                                                                                                                                          |
| B11 | `portal_field_maps.sort_order`: number rows by `(created_at,id)` within `(org_id,portal_key,page_step)`; fill NULL only.                                                                                                                                                                                                                                                                | Separate shared and org tiers; duplicate `(portal_key,selector)` shared groups and `(org_id,portal_key,selector)` org groups must be zero before unique indexes. Never delete/dedupe automatically or overwrite trained maps/order.                                                                                                                                                                                 |
| B12 | `sop_templates.states=ARRAY[state]` only when `states` is NULL and scalar state is non-null.                                                                                                                                                                                                                                                                                            | Preserve scalar state and existing arrays. Keep `All` as its own sentinel and NULL fallback as NULL. Validate shapes and active template overlap at `(org,payer,group,state)` before swapping uniqueness mechanisms; do not archive a conflicting template automatically.                                                                                                                                           |
| B13 | For existing online-form step data fields, retain original JSON, append the exact-content snapshot/version marker once, then create proposed/manual registry entries using the source's deterministic `manual:<md5>` selectors.                                                                                                                                                         | Inventory affected templates, tenant ownership, existing versions and proposed selectors first. Source inserts shared rows regardless of template org: stop if that would expose tenant-private definitions. No-portal steps remain unresolved instead of guessing. Fail on conflicting version/selector content; `ON CONFLICT DO NOTHING` alone is not enough proof. Repeat should add zero further versions/maps. |
| B14 | Add the missing enrollment, payer-contact, payer-form, verification, vault and intake tables empty. Add profile split fields NULL.                                                                                                                                                                                                                                                      | No production row copies, fake enrollment histories, preverified fields, SSN reconstruction, Auth-user creation, or UAT fixture insertion. Newly added relationships/history under B3/B5 are transformations of existing staging records, not test-data imports.                                                                                                                                                    |

Database-maintained `updated_at` changes caused by the new backfills must be named
in the allowed-column manifest. They must not conceal unrelated edits. Capture
unchanged-column hashes per primary key before/after; table row counts alone cannot
prove preservation.

## Historical operations explicitly excluded

| File / operation                                                                 | Disposition                                                                                                                                                                                                                                                                                                                |
| -------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `20260704210000_baseline_live_schema.sql` and blanket history replay             | Exclude. Existing object definitions and managed owners are not an empty database; the ledger is not a missing-file to-do list.                                                                                                                                                                                            |
| `20260717221914_payer_dead_column_drop_superseding.sql`                          | Exclude all 11 payer-column drops and `credential_cases.payer_provider_id` drop. Keep the 12 legacy columns dormant; counts intentionally differ from production.                                                                                                                                                          |
| Both `.sql.superseded` files                                                     | Never executable. No reactivation to repair filename/version gaps.                                                                                                                                                                                                                                                         |
| `20260810120000_purge_unreferenced_catalog_payers.sql`                           | Exclude payer deletion, even when a row appears unreferenced.                                                                                                                                                                                                                                                              |
| `20260807120000_org_intake_no_default_sales_rep.sql` cleanup                     | Exclude top-level assignment/party DELETEs. Current RPC definitions supply future behavior without altering existing records.                                                                                                                                                                                              |
| `20260807130000_people_contact_roles.sql` orphan cleanup                         | Replace the unresolved-party DELETE with a fail-closed precondition and explicit remediation list.                                                                                                                                                                                                                         |
| Early `commit_import_run` definitions (`20260713191000` and any baseline copies) | Exclude. Already-applied CSV parity must keep its exact body and ACL; prerequisite DDL cannot downgrade it.                                                                                                                                                                                                                |
| Original `20260713170000_case_generation_run_rows.sql` table definition          | No replay needed. Its old created-case CHECK is inside `CREATE TABLE IF NOT EXISTS`; **on the existing table this does not re-add that CHECK**. E6.3 widens only disposition/reason checks. Preserve the later repair with explicit readback/tests rather than claiming an existing-table replay reinstates the old CHECK. |
| `20260903210000_delete_case_rpc.sql` and `20260921220000` repair                 | Already installed behavior: compare and preserve rather than replay solely because hosted versions differ. Add missing dependencies, then test on owned disposable cases.                                                                                                                                                  |
| Production-only Aetna operator tables                                            | Exclude `portal_field_maps_aetna_backup_20260904`, `portal_field_maps_aetna_direct_backup_20260812`, `portals_aetna_backup_20260904`, their rows and grants.                                                                                                                                                               |
| `supabase/seed.sql`, `supabase/seed-uat.sql`, Auth seed and key material         | Exclude from schema/data alignment and all production artifacts. The deterministic UAT pack remains independently guarded staging/local tooling.                                                                                                                                                                           |

## Rehearsal, hosted apply and acceptance

1. Build the new delta from the qualified restore and reviewed object manifest.
   Rehearse once, verify every B1–B14 predicate, then repeat it to demonstrate
   preservation/idempotence. Simulate failure between transactions and resume;
   repeat means no duplicate histories, facilities, defaults, versions or maps.
2. Verify all 65 original cases and every existing table's primary keys remain;
   compare unchanged-column hashes and relation sets. Account explicitly for
   approved status/date/number/name/default/version backfills. Preserve existing
   Auth users, memberships, assignments, ledgers and legacy UAT records.
3. Exercise real PostgreSQL authorization: member, nonmember, wrong-org, admin,
   specialist, billing and anonymous. Include P01 negative tests, corrected
   provider-verification tenant checks, contact composite FK, immutable documents,
   append-only ledgers, final payer RPCs and Storage path policies. Synthetic probe
   writes belong to owned disposable records in the isolated rehearsal first.
4. Rehearse CSV commit parity and `delete_case` with its full dependencies. For a
   generated disposable case, deletion must preserve its run row, null its FK,
   preserve history reason, and add the intended audit. A created run-row INSERT
   without a case must still fail. Assert the CSV/delete function hashes/ACL and
   generated-case trigger/FK are unchanged from the qualified baseline.
5. After green exact-head CI and independent delta review, refresh staging identity
   and all preflight counts/hashes under the bounded application-write boundary.
   The approved CLI maintenance window alone does not prove application writes
   are paused. Abort on prestate drift; do not widen scope automatically.
6. Apply only checksummed staging slices, record source checksum ↔ hosted migration
   version, and read back every changed definition and policy. Do not rewrite
   historical ledger versions merely to match filenames. The duplicate historical
   `20260903210000` source version remains a separate lineage issue.
7. Qualify hosted Auth/PostgREST/Storage/vault behavior with staging-owned personas
   and the pinned deployment/database binding. A database-only restore or mocked
   browser test is insufficient. Add deterministic UAT only if required for a named
   scenario; preserve existing UAT. Record source SHA, schema manifest, deployment
   ID, identities/roles, scenario results and cleanup receipts without secrets.
8. On failure, stop further slices and app activation; preserve additive structures.
   Use reviewed forward corrections or qualified staging recovery, not table drops,
   production restores, or restoring the vulnerable P01 predicate. Present the
   production packet separately after staging evidence is complete.

## Remaining uncertainty — must be resolved before calling AUD-02 complete

| Open item                                           | Required resolution                                                                                                                                                                 |
| --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Restore qualification                               | Exact baseline recovery/integrity receipt; current recovery work is outside this read-only review.                                                                                  |
| Contact ownership/defaults                          | Counts of unresolved/shared parties and conflicting defaults; reviewed split/relink map if needed.                                                                                  |
| Contracts, insurance, payer names, document history | Actual conflicting/eligible counts and post-transform expectations; no inferred zeroes.                                                                                             |
| SOP/registry existing content                       | Template-overlap, duplicate-selector, version-collision and shared-vs-private classification audits; B13 may require a scoped variant.                                              |
| Full schema manifest                                | Index/default/sequence/extension/owner/ACL inventory and explanation of remaining function hash differences.                                                                        |
| Storage coverage                                    | Need and exact contract for legacy `form-templates`; independent staging Storage/API and signed-URL checks. No repository bucket source was found.                                  |
| Security scope                                      | Existing broad shared-catalog authoring permissions are historical policy; this alignment does not silently harden or expand them. Record separately any newly exposed unsafe path. |
| Runtime completion                                  | Hosted Auth/RLS, vault, CSV, documents, case/facility/deletion, deployment binding and installed-extension acceptance remain separate proofs.                                       |

The plan is complete enough to derive a reviewed delta once these preconditions
are measured. It is not an assertion that every historical migration is safe to
run, that restore is qualified, or that production needs staging/UAT data.
