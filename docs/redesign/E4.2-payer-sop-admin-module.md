---
epic: e4.2
title: Payer & SOP Admin Module — Upstream Configuration Space
stage: 4
release: R6
status: reviewed
owner: devin
reviewed: true
---

# E4.2 — Payer & SOP Admin Module: Upstream Configuration Space

## Purpose and Context

`[r6]` decision A0: **payer SOPs are a mandatory prerequisite for system
readiness** — case generation must be entirely deterministic, driven by
upstream configuration rather than downstream guesswork. This epic builds a
**dedicated administrative module for Payer and SOP management**, owned by a
designated configuration user (Ops Lead or Admin), where payer-specific
requirements, logic, and task sequences are defined BEFORE cases exist. With
SOP setup centralized upstream, generated cases arrive with complete,
accurate task checklists, and specialists log in to a clean, high-density
queue where they execute immediately — never investigating missing case
details.

The PM's "micro-frontend" architecture note is **confirmed** (PM
2026-07-14) to mean a **user-role-controlled admin module inside the
existing app** — NOT a separately deployed application: a cleanly
separable, role-gated route subtree with no coupling into specialist
workflow components. The reviewer records the concrete isolation mechanism
in this single-app TanStack Start codebase in §5.

The module also becomes the home for the vocabularies and settings R6
introduces: denial/return reason codes (E4.0) and the org-level
next-best-action queue settings (resolved `[r6]` NBA-configurability
decision, PM 2026-07-14: admin-configurable with a shipped default, at the
organization level).

## Personas Empowered

- **P4 Company Owner / Ops Lead** — the designated configuration user; owns
  payer + SOP setup. (First epic where P4 becomes an operating actor; a P4
  seed row is added per seed-universe's open item.)
- **P1 Credentialing Manager** (admin role) — alternate configuration owner.
- **P2 Credentialing Coordinator / Specialist** — indirect beneficiary:
  cases always arrive fully specified.

## Component & Data Constraints

- Built from the current component library per
  [uiux-component-guide.md](./uiux-component-guide.md); scenario data from
  [seed-universe.md](./seed-universe.md).
- Reuses the shipped SOP-as-data model (E1.7b, Model A versioning) and the
  global payer catalog (E1.6) — this epic is a **management surface**, not a
  new SOP or payer model.
- Access is role-gated (admin/config role); RLS unchanged; additive schema
  only for readiness flags/vocabularies. Reviewer records mechanisms in §5.

---

## Features

### F4.2.1 — Dedicated admin module for payers & SOPs

- **Description:** A distinct admin space (own nav entry + route subtree,
  admin/config-role gated) consolidating: the payer directory with per-org
  attachment state (E1.6/E1.5), SOP templates with versions and match keys
  (E1.7b), and the mapping payer × state × specialty → SOP. Existing
  scattered admin surfaces for these move under (or link into) this module.
- **Persona:** P4, P1.
- **Benefit hypothesis:** one owned configuration space (A0) — the config
  user sees and maintains everything case generation depends on in one
  place.
- **Acceptance criteria:**
  - The module lists payers with their SOP coverage and attachment state.
  - **Per-payer resolution-identifier config:** each payer's config holds
    the label of its payer-issued enrollment identifier (e.g. "Provider
    PIN" for Aetna, "Provider ID" for BCBS) and whether one is expected
    at approval; unconfigured payers fall back to a generic
    "Payer-issued ID" optional field. Consumed by E4.0 F4.0.3's approval
    step.
  - SOP create/edit/version flows are reachable from the module and
    unchanged in behavior (E1.7b rules hold).
  - **Draft state (PM round-4):** the SOP wizard supports **save as
    draft** — an unpublished work-in-progress that persists across
    sessions and is visible/editable by other config users, so a
    half-encoded payer is never lost and can be handed off. Drafts never
    resolve for generation and never count toward readiness (F4.2.2);
    publishing still follows E1.7b unchanged.
  - **Minimum-content publish lint (PM round-4):** Publish rejects an
    empty or placeholder SOP — at least one task, every task has at least
    one step, and no default "New step"/blank labels — so a payer can
    never flip to Ready on a hollow template.
  - **Global-tier publish gate (PM round-4):** publishing a change to a
    **global/shared** template (consumed by every org without an
    override) shows a publish-diff preview and an explicit
    blast-radius confirmation before the version lands; org-specific
    templates publish as today.
  - **Accessible reordering (PM round-4):** task and step reordering in the
    SOP wizard must not depend on drag-and-drop alone — the shipped
    drag-only reorder was not a functional experience in production. Each
    task/step row offers explicit move up/down controls (buttons,
    keyboard-operable, proper focus/aria) alongside drag; reordering is
    fully achievable without a pointer.
  - Non-admin/config roles cannot access the module (role-gated route).
  - The module is a cleanly isolated route subtree with no imports from
    specialist workflow components (isolation requirement; §5 records the
    mechanism). The boundary is **bidirectional and enforced, not
    conventional** (PM/Gemini round-2): specialist workflow code never
    imports from the admin module either, verified by an automated check
    (lint rule or boundary test) so the SOP rule engine can evolve without
    regressing the daily execution queue.
  - **SOP task Execution Type metadata:** in SOP task edit flows the config
    user assigns each task an execution type — `manual` (default),
    `extension_fill`, `auto_verify`, `document_attach`. In R6 this is
    **captured configuration** stamped onto generated case tasks; the
    automated behaviors activate in their owning epics (`extension_fill`
    → E4.3 workbench payload tee-up; `document_attach` → E4.5 vault pull;
    `auto_verify` → R7 automation). Existing SOP versions/tasks default to
    `manual`; publishing rules (E1.7b) are unchanged.
  - **Template tier visibility:** the SOP list and each resolution surface
    show which tier a template row is — org-specific or global/shared
    (payer-level, visible to all orgs) — matching the shipped
    `pickTemplate()` precedence (exact → payer+state → global fallback):
    orgs share global payer templates unless a customer-specific one
    exists, and the admin can create the org-specific override from the
    payer's row.
- **Test/data scenario(s):** TS-76, TS-95.

```gherkin
Feature: Payer & SOP admin module
  Scenario: Config user manages SOP coverage in one space
    Given P4 signs in with the config/admin role
    When she opens the Payer & SOP admin module
    Then she sees payers with attachment state and SOP coverage per state
    And can open a payer's SOP to edit tasks, producing a new version per E1.7b
```

### F4.2.2 — Payer readiness ("SOP baseline") signal

- **Description:** Each attached payer × state shows a derived readiness
  signal: **Ready** (a current SOP resolves for it) or **Needs SOP** (only
  the generic fallback would resolve). Generation previews (E2.0) and the
  case surfaces label fallback-resolved work as before; the module shows the
  gap list so the config user closes it upstream.
- **Persona:** P4, P1.
- **Benefit hypothesis:** A0's "SOPs as baseline for system readiness"
  becomes visible and actionable — the config user can drive coverage to
  100% before generation, keeping cases deterministic.
- **Acceptance criteria:**
  - The module lists every attached payer × state with Ready / Needs SOP,
    fully derived (no stored readiness).
  - The Needs-SOP list links straight into SOP creation with the match key
    prefilled.
  - No blocking behavior change to generation (fallback SOP still applies
    per [r4]); this is an upstream visibility surface.
  - **Scorecard connection (PM 2026-07-14):** readiness (upstream config
    completeness) and the existing payer scorecard (downstream performance
    — mapping coverage, first-pass rate, time-in-bucket) are complementary
    views of the same payer. Each payer row in the module links to its
    scorecard, and the scorecard gains a derived **SOP readiness**
    indicator (same pure projection — no stored readiness), so config gaps
    and performance read side-by-side.
  - **Form readiness (PM 2026-07-14):** for a payer whose resolved SOP
    contains `extension_fill` tasks, readiness ALSO surfaces the portal's
    **mapping coverage** (approved ÷ approved+proposed field maps — the
    same shipped scorecard derivation) beside Ready/Needs-SOP, with a link
    into form training. A payer can therefore be "SOP Ready" but "Form
    untrained" — both are visible before generation, so form training is a
    setup step, not a fill-time surprise.
- **Test/data scenario(s):** TS-77, TS-98.

### F4.2.3 — Reason-code vocabulary management

- **Description:** The E4.0 denial/return reason-code list is managed here:
  the shipped defaults (Missing Documentation, Network Closed, Demographic
  Mismatch, Incomplete Application, Credentialing Criteria Not Met, Other)
  plus org-added codes; codes deactivate (never delete) so historical
  resolutions keep their meaning.
- **Persona:** P4, P1.
- **Benefit hypothesis:** structured vocabularies stay curated by the
  config owner instead of drifting via free text (A4).
- **Acceptance criteria:**
  - Defaults are seeded and non-deletable; org codes can be added and
    deactivated.
  - Deactivated codes disappear from new-entry dropdowns but render on
    historical records.
- **Test/data scenario(s):** TS-78.

### F4.2.4 — Bulk case generation entry point (new-contract flow)

- **Description:** The module (and the case-generation surface) exposes the
  **bulk generation** workflow A1 requires: a group signs a new contract /
  attaches a new payer, and the config user or manager generates cases for
  ALL eligible providers under that group × payer × state in one run — this
  is the existing E2.0/E2.1 preview → generate machinery, entered from the
  payer/contract context with the payer preselected. Ad hoc one-off creation
  (misses found during onboarding) remains via the existing manual case flow.
- **Persona:** P1, P4.
- **Benefit hypothesis:** new-contract onboarding becomes one deterministic
  bulk run with the E2.0 preview/exclusion safety net, not N manual
  creations.
- **Acceptance criteria:**
  - From a payer (or its attachment) in the module, "Generate cases" opens
    the E2.0 preview scoped to that payer; generation follows E2.1 rules
    (4-part key, dedupe, exclusions) unchanged.
  - **Release configuration (SLA protection, PM/Gemini round-2):** the
    preview lets the operator release **all**, **none**, or a **filtered
    subset** (by provider selection, count cap, or location) in this run —
    a large new contract need not dump every case into Drafting at once.
    Unreleased candidates remain eligible and visible for a later run;
    each run is recorded with existing E2.4 traceability and the release
    scope it used. No generation rule ([r4]-locked candidate/dedupe/
    exclusion logic) changes — this is a selection layer on the preview.
- **Test/data scenario(s):** TS-79, TS-97.

### F4.2.5 — Next-best-action queue settings (org-level, with default)

- **Description:** An admin-only settings surface for the E2.3/E4.1 "My
  Cases" queue: the org admin can reorder and enable/disable the ranking
  inputs (overdue follow-ups, task due dates, provider start dates,
  location launch dates) for their organization. The system ships a
  default configuration (overdue follow-ups above deadline-only rows;
  ties by earliest deadline); "Reset to default" is always available.
  Config is org-scoped; the queue derivation reads it live — no per-case
  or per-user priority is ever stored.
- **Persona:** P4, P1 (admins); P2 experiences the resulting ordering.
- **Benefit hypothesis:** orgs differ in what "most urgent" means; an
  org-level dial with a sane default gives control without sacrificing
  the deterministic, fully derived queue.
- **Acceptance criteria:**
  - With no saved config, the queue uses the shipped default; saving a
    config changes ranking for that org only, immediately, for all users.
  - Reordering/enabling inputs and Reset-to-default are admin-only;
    changes are audited.
  - The queue remains fully derived; the config is an input to the
    derivation (E1.0 derived-progress rule holds).
- **Test/data scenario(s):** TS-91.

### F4.2.6 — Upstream profile gating ("phantom provider" fix)

- **Description:** An SOP can declare **required provider-profile
  attributes** (from a governed list of existing profile fields — e.g.
  CAQH ID, license number/expiration, NPI, malpractice coverage fields)
  that a provider must satisfy before a case generates against that SOP.
  During generation preview, providers failing the gate are surfaced as
  **blocked with the specific missing attributes** (riding the existing
  per-provider skip/exclusion machinery) instead of generating a case
  that stalls mid-pipeline; the operator can spawn a **provider-outreach
  task** (existing task machinery, title prefilled with the missing
  attributes) per blocked provider from the preview.
- **Persona:** P4, P1 (configure + run); P2 benefits (no phantom cases).
- **Benefit hypothesis:** cases stall today because obscure payer-specific
  profile data is discovered downstream; gating moves the discovery
  upstream where it's a data-collection task, not a stalled case.
- **Acceptance criteria:**
  - Attribute requirements are configured per SOP (versioned with it,
    E1.7b rules unchanged) from a governed field list — no free-text
    field names.
  - Preview shows gated providers with the exact unmet attributes;
    generation never creates a case for a gated provider in that run.
  - Spawning the outreach task is offered, never automatic-silent; the
    task references the provider and missing attributes.
  - Once the profile is completed, the provider passes the gate in the
    next preview — no manual unblocking state is stored.
  - **Blocked-provider visibility (PM round-4):** the payer's readiness
    surface (F4.2.2) shows the derived count of currently gated providers
    for that payer × state, linking to the list with their missing
    attributes — so ignored outreach never leaves providers blocked
    invisibly.
- **Test/data scenario(s):** TS-96.

### F4.2.7 — Form onboarding & test runner (dummy-provider dry run)

- **Description:** SOP setup gains an explicit **form onboarding** workflow
  for payers whose SOP includes `extension_fill` tasks, replacing today's
  implicit "map fields whenever the extension happens to see the form":
  1. **Intake/scrape:** from the payer's portal registry row, the operator
     opens the live form with the extension in capture mode; observed
     fields land as org-scoped `proposed` field-map rows (existing
     capture contract — no new write path).
  2. **Train:** the existing training deck (`/portals/$key/train` —
     confidence-batched approve/edit/manual, dictionary suggestions,
     undo) resolves the proposals; coverage rises on the readiness signal
     (F4.2.2) in real time.
  3. **Test run:** the operator executes a **dry-run fill against a
     designated test provider** (the "Sowmya dummy provider" pattern,
     made first-class): the extension fills the live form from the test
     provider's profile, the operator watches field-by-field, and the
     run reports filled / skipped-unmapped / empty-token per field.
     Nothing is submitted to the payer and no case is required.
  4. **Fix and re-run:** unmapped/empty results link straight back to
     training or the test provider's profile; re-run until coverage is
     acceptable, then the payer's form readiness reflects it.
- **Persona:** P4, P1.
- **Benefit hypothesis:** form-fill throughput is testable at setup time
  with zero real cases — gaps are found once by the config user, not
  1,000 times by specialists in the Fix-It queue.
- **Acceptance criteria:**
  - A designated org-scoped **test provider** exists (seeded/created,
    clearly labeled, excluded from queues, generation candidates, and all
    scorecard/reporting derivations).
  - Test-run fill sessions are recorded and **distinguishable** from real
    fills (additive marker) and excluded from first-pass-rate and any
    operational metric.
  - The per-field result of a test run (filled / skipped-unmapped /
    empty-token) is visible to the operator with a one-click path to the
    fix (training for unmapped; profile edit for empty tokens).
  - No new extension write scope: the runner uses the existing read-only
    payload + fill-event logging contracts; nothing is submitted.
  - **Fill-time re-capture (PM round-4):** during any fill — test or
    real — form fields the extension observes that have **no field map**
    land as org-scoped `proposed` field-map rows through the same capture
    contract, so every fill trains the form instead of losing the
    observation; the training deck picks them up like setup-time
    captures.
  - Real-fill behavior and Fix-It derivation are unchanged — the runner
    front-loads the same loop at setup time.
- **Test/data scenario(s):** TS-99.

---

## Scenario/Seed Data Mapping

| Test scenario | Org(s)/User(s)                             | Purpose                                               |
| ------------- | ------------------------------------------ | ----------------------------------------------------- |
| TS-76         | Dillon fixtures; new P4 seed row (L3 + L1) | Role-gated module, payer/SOP consolidation            |
| TS-77         | Payer attached with no payer SOP (L1)      | Derived Ready / Needs-SOP signal + prefilled SOP link |
| TS-78         | Org-added reason code (L1)                 | Vocabulary add/deactivate; historical rendering       |
| TS-79         | New payer attachment on a group (L1)       | Bulk generation entry scoped to the payer             |
| TS-91         | P4 reorders queue inputs; P2's queue (L1)  | Org-level NBA config with default + reset; derived    |
| TS-95         | SOP task execution types (L1)              | Execution type set per task; stamped on generation    |
| TS-96         | Provider missing gated attribute (L2)      | Preview blocks with reasons; outreach task spawned    |
| TS-97         | Large-contract staggered release (L2)      | Filtered release subset; remainder eligible later     |
| TS-98         | Form readiness beside SOP readiness (L1)   | Mapping coverage shown for extension_fill SOPs        |
| TS-99         | Dummy-provider test-run fill (L2)          | Dry run: per-field results; excluded from metrics     |

## Dependencies

- E1.6 payer catalog, E1.5 attachment, E1.7b SOP-as-data (Model A),
  E2.0/E2.1/E2.4 generation machinery.
- E4.0 reason codes (F4.2.3 manages that vocabulary).
- Seed-universe open item: P4 seed row (added by this epic's TS-76).
- Resolved `[r6]` NBA-configurability decision (org-level queue settings
  live here as F4.2.5, consumed by E4.1 F4.1.3).

## Out of Scope / Non-Goals

- No new SOP model or versioning semantics (E1.7b Model A is locked).
- No separate-deployment micro-frontend — PM confirmed 2026-07-14 that
  "micro-frontend" means the role-controlled admin module inside the
  existing app.
- No payer-portal credential storage.
- No changes to generation rules themselves ([r4]/[r4-review] locked) —
  F4.2.4's release configuration and F4.2.6's gate are selection/visibility
  layers over the preview, not new candidate/dedupe/exclusion logic.
- No auto-execution engine in R6: execution-type metadata is captured here;
  `extension_fill` behavior activates with E4.3, `document_attach` with
  E4.5, `auto_verify` is an R7 automation.
- **No mid-flight SOP version adoption (PM round-4):** in-flight cases stay
  grandfathered on the version they generated with; a published fix applies
  to future generation only. No adopt/migrate path is built.

## Revision History

| Date       | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Author |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 2026-07-14 | Initial draft from [r6] decisions A0/A1                                                                                                                                                                                                                                                                                                                                                                                                                                          | devin  |
| 2026-07-14 | NBA-configurability decision applied: added F4.2.5 org-level queue settings with shipped default (TS-91)                                                                                                                                                                                                                                                                                                                                                                         | devin  |
| 2026-07-14 | PM confirmed “micro-frontend” means a role-controlled module inside the existing app                                                                                                                                                                                                                                                                                                                                                                                             | devin  |
| 2026-07-14 | Reviewed and technically enabled                                                                                                                                                                                                                                                                                                                                                                                                                                                 | devin  |
| 2026-07-14 | PM use cases applied: per-payer resolution-identifier config (label + expectedness, e.g. Aetna "Provider PIN" / BCBS "Provider ID") added under F4.2.1, consumed by E4.0 F4.0.3                                                                                                                                                                                                                                                                                                  | devin  |
| 2026-07-14 | PM/Gemini round-2 applied: enforced bidirectional module import boundary (F4.2.1/TE-1); SOP task Execution Type metadata, phased — captured in R6, behaviors ride E4.3/E4.5/R7 (F4.2.1/TE-12); F4.2.6 upstream profile gating with outreach-task spawn (TE-13); F4.2.4 staggered release configuration (TE-14); template tier (global vs org-specific) surfaced, matching shipped pickTemplate precedence. TS-95–TS-97 added.                                                    | devin  |
| 2026-07-14 | PM connections: F4.2.2 readiness ↔ existing payer scorecard linked (module row → scorecard; scorecard gains derived SOP-readiness indicator); clarified F4.2.4 vs E3.x imports (imports create scope entities, generation creates payer work — different layer, no duplication); global settings + role/capability administration added to R7 roadmap                                                                                                                            | devin  |
| 2026-07-14 | PM round-3 (form onboarding): F4.2.2 gains form readiness (mapping coverage beside Ready/Needs-SOP for extension_fill SOPs, TE-16); new F4.2.7 form onboarding & test runner — capture → train → dummy-provider dry-run fill with per-field results, excluded from metrics (TE-17). TS-98/TS-99 added.                                                                                                                                                                           | devin  |
| 2026-07-14 | PM round-4 (chokeholds): F4.2.1 gains draft state, minimum-content publish lint, global-tier publish gate, accessible reordering (move up/down, keyboard-operable); F4.2.6 blocked-provider visibility on readiness; F4.2.7 fill-time re-capture (unmapped fields observed during any fill land as proposed field-map rows); non-goal recorded: no mid-flight SOP version adoption (grandfather). Payer onboarding runbook added at `docs/redesign/payer-onboarding-runbook.md`. | devin  |
| 2026-07-16 | Independent review session (per the PM request and `docs/redesign/REVIEW-HANDOFF.md`), §5 amendment: PM-approved Payers-nav consolidation recorded as **TE-18–TE-21** (one "Payer Setup" sidebar destination at `/admin/payer-admin` replacing the overlapping "Payer Management" + "Payer & SOP Setup" items; workspace presentation of Setup / Catalog / SOP templates / Forms & portals / Organization settings over existing routes; deep-link safety; empty-state/non-admin/keyboard/E2E gates; prohibitions). Reviewer redlines appended. No other section changed; `reviewed: true` untouched (the flag remains the PM's call).                                          | claude |

---

## 5. Technical Considerations & Enablers

_Reviewer-owned. Verified 2026-07-14 against the existing payer administration
(`src/routes/admin.payers.tsx`, `src/services/payers.ts`), SOP administration
and immutable publishing (`src/routes/admin.templates.index.tsx`,
`src/components/templates/TemplateWizard.tsx`,
`src/services/templates.ts`, `src/hooks/useAdmin.ts`,
`src/lib/pickTemplate.ts`), payer-network targeting
(`src/services/payerNetworkTargets.ts`,
`src/hooks/usePayerNetworkTargets.ts`), and the E2 generation flow
(`src/routes/generation.tsx`, `src/services/generationPreview.ts`,
`src/services/generationConfirm.ts`). The module is a consolidation and
configuration boundary over those shipped domains, not a replacement payer,
SOP, or generation engine._

- **TE-1 — Implement “micro-frontend” as a bounded route/module inside the
  existing TanStack Start application.** Give the workspace its own
  `/admin/...` route namespace and module-local components under
  `src/components/payer-admin/`, but keep one Vite/TanStack Start deployment,
  shared auth store, query client, design system, and service layer. Compose or
  relocate the existing payer and template screens rather than cloning them.
  A separately built or deployed frontend would duplicate authentication,
  organization state, navigation, and data contracts and is outside the
  confirmed PM interpretation. F4.2.1's explicit “own nav entry” requirement
  authorizes one narrowly scoped protected-file change in
  `src/components/layout/Sidebar.tsx` for the admin-only link; no broader shell
  redesign is authorized.

- **TE-2 — P4 maps to the existing `admin` authorization role for R6.** The
  current role union is `admin | specialist | billing`; there is no `config`
  role. Seed the P4 Company Owner / Ops Lead persona with an `admin`
  membership, enforce the route with the existing admin guard, disable
  protected queries until that guard resolves, and keep module mutation
  controls admin-only. P2 (`specialist`) is denied at the route boundary.
  Per the epic's explicit “RLS unchanged” constraint, existing payer/SOP table
  policies are not narrowed here; that leaves a known defense-in-depth gap if
  “configuration user only” is later intended as a database-enforced security
  boundary. Do not add a fourth role or capability system unless the PM
  separately requires config-only users.

- **TE-3 — Reuse payer and SOP services; preserve published-version
  semantics.** Payer reads/writes remain in `payers.ts`; SOP head/version
  reads and the `publish_sop_template_version` RPC remain in `templates.ts`.
  The module must use the existing optimistic-concurrency and immutable-version
  behavior and the centralized `pickTemplate()` precedence. No component calls
  Supabase, no second template resolver is introduced, and no in-flight case is
  silently moved to a newer SOP version.

- **TE-4 — Readiness is a pure projection over attached payer targets and
  payer-specific SOP resolution.** Start from active
  `payer_network_targets`/organization payer assignments, group by attached
  payer × state for display, and evaluate every underlying group target through
  the existing resolver. “Ready” means every active target resolves to an
  active payer-specific SOP; a global generic fallback is deliberately shown
  as “Needs SOP.” If any grouped target is uncovered, the aggregate row is
  “Needs SOP” and the creation link carries the payer/state/group match inputs.
  Do not persist a readiness boolean; invalidate payer-target and template
  query prefixes after configuration writes.

- **TE-5 — Add one organization-scoped reason-code vocabulary, not enum text
  in components.** An additive `denial_reason_codes` table should carry a
  stable ID, `org_id`, machine code, label, `is_system`, `is_active`,
  `created_by`, and timestamps, with case-insensitive uniqueness per
  organization. Seed the default codes for every organization, including the
  organization-bootstrap RPC path. System defaults cannot be deleted;
  organization codes are deactivated, not deleted. Historical denial rows
  reference the stable ID and continue resolving inactive labels. Provide
  select/insert/update policies for admin members only, no delete policy, and
  explicit grants. E4.0 owns the eventual denial-event foreign key after its
  pipeline contract is clarified.

- **TE-6 — Bulk generation is an entry point into the E2 flow.** Add an
  optional payer/group scope to the existing generation route, preview input,
  and pure candidate filter. Launching “Generate cases” from payer context
  pre-filters the same E2.0 preview; confirmation still uses the same E2.1
  transaction/RPC and E2.4 run traceability. Preserve the locked candidate,
  exclusion, dedupe, reapplication, and audit rules. Do not create a parallel
  bulk generator or insert cases directly from the admin module.

- **TE-7 — Add one organization-level queue configuration row with a code
  default.** An additive `next_best_action_configs` table should use `org_id`
  as its primary key and store an exact permutation of the four fixed input
  keys—`follow_up`, `task_due`, `provider_start`, and `launch_date`—plus a
  subset of disabled keys, with `updated_by` and `updated_at`. `follow_up`
  governs both explicit next-follow-up and existing SOP-cadence touch
  deadlines. Database/service validation rejects unknown or duplicate keys.
  Absence of a row means the shipped comparator: arrived/overdue follow-ups
  first, then remaining signals by earliest deadline; “Reset to default”
  deletes the row after an audit write. Keep the default and input-key union in
  one pure TypeScript module shared by the admin service and
  `buildNextBestActions`; do not store a per-case score, priority, or queue
  row. Admin writes are organization-scoped, while authenticated organization
  members may read the effective setting used by their queue.

- **TE-8 — Query ownership remains organization-scoped and domain-specific.**
  Continue using TanStack Query hooks over services and shared keys in
  `src/hooks/queryKeys.ts`. Mutations invalidate only the relevant
  organization prefixes: payers/assignments, payer-network targets, templates
  and versions, reason codes, queue configuration/queue inputs, generation
  preview, and audit log. A successful queue-setting save/reset immediately
  reranks every user through the existing derived hook. Organization switching
  continues to clear cached server state through the existing auth store;
  Zustand is not used as a second database cache.

- **TE-9 — Preserve existing RLS while securing additive configuration
  data.** The epic explicitly leaves current payer/SOP RLS unchanged; retain
  those policies and enforce this consolidated surface through the admin route
  and module mutations. The new reason-code table is organization-scoped,
  permits admin configuration writes, has no delete policy, and remains
  readable where operational/history rendering requires labels. Queue-config
  rows permit organization-member reads and admin insert/update/delete so reset
  can remove the override. Every insert derives `org_id` from the active
  organization, every mutation writes audit, and all new public tables receive
  explicit least-privilege grants. Record the existing specialist-capable
  payer policy as technical debt rather than silently changing authorization
  scope in this epic.

- **TE-10 — UI uses a high-density table/workspace composition.** Reuse the
  approved `Table`, `Tabs`, `Badge`, `Select`, `Dialog`, `Input`, and `Button`
  primitives with custom cells for attachment/readiness pills and two-line SOP
  coverage. Queue settings use existing checkbox/toggle controls plus
  accessible move-up/move-down buttons rather than adding a drag-and-drop
  dependency. Use one-pixel borders, `rounded-md`, compact rows/padding,
  approved tokens, and no card shadows, gradients, or decorative backgrounds.
  Every form control has an explicit label; icon-only controls have labels and
  tooltips. Apart from the TE-1 sidebar link, no protected layout change is
  authorized; no primitive file is modified.

- **TE-11 — Test the boundary and reuse contracts.** Unit/service tests cover
  grouped readiness, fallback-only “Needs SOP,” inactive reason-code history,
  queue-config validation/default/reset and cross-org isolation,
  route/service/RLS authorization, and payer-scoped generation filtering.
  Playwright scenarios TS-76–TS-79 and TS-91 must verify P4 as seeded admin, P2
  denial, SOP-link prefilling, reason-code deactivation, queue reranking/reset,
  and handoff into the existing generation preview/run history. Add no live
  payer or extension dependency.

- **TE-12 — Execution type is additive SOP-task metadata, stamped at
  generation.** Add a nullable `execution_type` (check-constrained to
  `manual | extension_fill | auto_verify | document_attach`, null ⇒
  `manual`) to the SOP task-definition rows carried inside versions and,
  additively, to generated case tasks so the value is stamped per E1.7b
  version immutability (a published version's tasks are never mutated —
  changing a type produces a new version). R6 renders the type (badge in
  SOP edit + case task rows) and stamps it; NO automated execution runs
  here — E4.3 reads `extension_fill`, E4.5 reads `document_attach`,
  `auto_verify` is R7. Keep the union in one shared TypeScript module;
  widen additively only.

- **TE-13 — Profile gating rides the existing preview skip machinery.**
  Requirements are a governed key list (one pure module mapping keys to
  existing provider-profile fields — no free-text paths), stored
  additively with the SOP version (same immutability as TE-12). The E2.0
  preview's pure candidate filter gains a gate evaluation producing
  per-provider unmet-attribute reasons rendered like existing
  skip/exclusion rows; the E2.1 generation transaction/RPC is unchanged
  (gated providers simply aren't in the confirmed set). The outreach task
  uses the existing task-creation service (org-scoped, prefilled title,
  provider reference), offered per blocked provider from the preview —
  never auto-created silently. No stored "blocked" state: the gate is
  re-derived every preview.

- **TE-14 — Release configuration is a selection layer on the preview,
  not a new generator.** The E2.0 preview keeps its full candidate list;
  the operator's release scope (all / none / subset by explicit provider
  selection, count cap, or location filter) narrows the confirmed set
  passed to the unchanged E2.1 RPC. The E2.4 run record additively carries
  the release scope used; unreleased candidates stay eligible and appear
  in the next preview via existing dedupe (no case exists → still a
  candidate). No queue of "pending releases" is persisted.

- **TE-15 — The module import boundary is machine-enforced.** Admin-module
  code lives under `src/routes/admin*` + `src/components/payer-admin/`;
  add an automated check (ESLint `no-restricted-imports` scoped config or
  a unit test walking import graphs) asserting no specialist workflow
  module (`src/components/cases/*`, `src/components/queue/*`, routes
  outside `/admin`) imports from `src/components/payer-admin/`, and the
  admin module imports specialist components only via shared `src/lib` /
  `src/services` / design-system primitives. The check runs in the normal
  test/lint gates so the boundary fails CI, not review memory.

- **TE-16 — Form readiness reuses the scorecard derivation.** The F4.2.2
  form-readiness ratio calls the SAME pure `mappingCoverage` computation the
  shipped payer scorecard uses (`src/lib/payerScorecard.ts`) over rows
  already fetched (portals + field maps) — no stored value, no second
  formula to drift. Shown only when the resolved SOP version contains at
  least one `extension_fill` task (a pure check over TE-12 metadata).

- **TE-17 — Test runner is additive over shipped fill plumbing.** A
  `fill_sessions` additive marker (e.g. `is_test boolean DEFAULT false` or
  an additive `fill_mode` value) distinguishes dry runs; every metric reader
  (scorecard `firstPassRate`, reporting) filters it out. The test provider
  is an ordinary `providers` row with an additive exclusion flag honored by
  queue/generation/scorecard derivations (one shared pure predicate, not
  per-surface checks). Per-field results ride the existing fill-event
  payload (`fieldsFilled`/`fieldsSkipped` — tighten the currently-loose
  `fieldsSkipped: unknown` typing to a structured
  `{selector, label, reason: "unmapped" | "empty_token"}[]` additively). No
  new extension write endpoints; the capture→proposed-row contract is
  unchanged.

### §5 Amendment — PM-approved Payers-nav consolidation (2026-07-16)

_Recorded by the independent review session (per
`docs/redesign/REVIEW-HANDOFF.md`), verified against the module as built on
`redesign` after PRs #170/#171: `src/routes/admin.payer-admin.tsx` /
`admin.payer-admin.index.tsx` / `admin.payer-admin.forms.$payerId.tsx`,
`src/components/payer-admin/*` (incl. `moduleBoundary.test.ts`),
`src/components/layout/Sidebar.tsx`, `e2e/sidebar-ia.spec.ts`,
`e2e/payer-admin-module.spec.ts`, and the E4.2 migrations
`20260715140000`–`20260715140600`, `20260716120000`, `20260716190000`. The
PM approved the amendment TE-18–TE-21 record. TE-18 supersedes TE-1's
sidebar sentence (which authorized ADDING one admin-only link beside "Payer
Management"); every other TE stands as written._

- **TE-18 — Sidebar authorization: ONE "Payer Setup" destination replaces
  the overlapping Payers items (PM-approved amendment, 2026-07-16).**
  `src/components/layout/Sidebar.tsx` (a protected
  `src/components/layout/*` file; a redesign epic's §5 is the only
  authorization to touch it, per `docs/redesign/uiux-component-guide.md`)
  may be edited **in its Payers section only**: the current two items —
  "Payer Management" → `/admin/payers` (all roles) and the admin-only
  "Payer & SOP Setup" → `/admin/payer-admin` (TE-1) — are replaced by a
  single **"Payer Setup"** item navigating to `/admin/payer-admin`. This
  supersedes ONLY the Payers half of E0.9 F0.9.3's route mapping ("Payer
  Management → `/admin/payers`"); with a single destination, F0.9.3's own
  "section labels only over 2+ item groups" rule applies (the Reporting
  Center precedent decides whether the "Payers" group label stays or the
  entry renders standalone — either way, only the Payers zone changes).
  **Visibility is admin-only**, matching the module's route gate and the
  TS-76 pin ("P2 has no nav entry"); specialist/billing users keep
  `/admin/payers` URL-reachable (read-only under the shipped E4.2
  governance) — see the open PM question in the review PR. **Everything
  else in the shell is preserved verbatim:** Workspace (Home, "My Cases"
  per E2.3 TE-8, Cases + open-case CountBadge), Reporting Center, the org
  zone (Account Detail, Facilities reserved → `/soon`, Providers →
  `/onboarding/wizard?section=providers` per E1.3 TE-11), the lifecycle-
  grouped switcher and its E0.0 TE-4 semantics, focus states, and the user
  footer. The implementing PR must cite **E4.2 TE-18** in the
  `Sidebar.tsx` header comment and in the updated e2e specs (TE-20),
  keeping E0.9/E1.3/E2.3 provenance for the rest of the IA.

- **TE-19 — The module presents Setup, Catalog, SOP templates, Forms &
  portals, and Organization settings as ONE workspace — composition over
  existing routes, never duplication.** The `/admin/payer-admin` subtree
  may gain the leaf routes/components needed to present the five areas:
  Setup (the shipped module home — payer directory with readiness, reason
  codes, queue settings), Catalog (the `/payer-directory` global-catalog
  surface, E1.6), SOP templates (`/admin/templates` list + wizard), Forms
  & portals (`/admin/portals` registry + the shipped
  `/admin/payer-admin/forms/$payerId` runner), and Organization settings
  (the existing org-scoped settings surfaces — `org_payer_settings` /
  assignment config and/or the `/admin/settings` panels; scope confirmed
  by the PM question in the review PR). Presentation means composing the
  EXISTING feature components and services — no second copy of a service,
  hook, resolver, or route logic; the TE-15 machine-enforced boundary
  (`moduleBoundary.test.ts`) must keep passing over any relocated
  component. **Deep-link safety:** `/admin/templates`, `/admin/portals`,
  `/payer-directory`, and `/admin/payers` each remain a working URL — as
  the live route or as an intentional, param-preserving redirect into the
  workspace (the `/admin/sops` → `/admin/templates` redirect-shell
  precedent) — honoring E0.9 F0.9.6's no-dead-ends rule. In-app inbound
  links (e.g. `ManualCaseModal` / `PayerNetworkSection` →
  `/payer-directory`; `TemplateTaskRow` / `PortalStepLink` →
  `/admin/portals`; the scorecard back-link → `/admin/payers`) must be
  re-pointed in the same PR if their target becomes a redirect.
  `/payer-directory` keeps its current non-admin browse behavior — the
  admin-gated workspace Catalog area does not remove the standalone
  page's browse access.

- **TE-20 — Consolidation gates: direct-fix empty states, explicit
  non-admin behavior, keyboard navigation, focused E2E.** (a) Every
  workspace area's empty state carries its direct fix, never a dead end:
  empty org payer set → add-from-Catalog; Needs-SOP → prefilled SOP
  creation (F4.2.2); empty portal registry → the registry/train path (the
  wizard's amber-note precedent); unsaved queue config → shipped-default
  explanation with Reset semantics. (b) Non-admin behavior is explicit
  and tested: the route-level denial stands (TS-76), the nav entry is
  admin-only (TE-18), and no role loses URL reachability to the four
  TE-19 routes. (c) Keyboard navigation: the new nav item is fully
  keyboard-operable with the F0.9.3 white-alpha focus ring; workspace
  tabs/areas are arrow-key traversable; TE-10's move up/down reorder
  controls remain pointer-free-operable. (d) Focused E2E in the SAME PR:
  `e2e/sidebar-ia.spec.ts` and `e2e/payer-admin-module.spec.ts` currently
  pin the superseded two-item Payers section ("Payer Management" visible
  to all; entry named "Payer & SOP Setup") — update those pins citing
  TE-18, add coverage for the single "Payer Setup" entry (admin sees it,
  specialist does not), the five workspace areas, and the four deep
  links/redirects (extend `e2e/legacy-routes.spec.ts` where redirects are
  chosen). Normal gates bind: `npm run lint`, `npm run test`,
  `npx tsc --noEmit`, and the touched Playwright specs.

- **TE-21 — Prohibitions (PM-approved amendment bounds).** The
  consolidation PR must NOT touch: SOP resolution (`src/lib/sopResolver.ts`
  — protected — and `src/lib/pickTemplate.ts` precedence, the
  `publish_sop_template_version` semantics, TE-3); any RLS policy, grant,
  or `SECURITY DEFINER` function; the `credential_cases` 4-part uniqueness
  key (`20260713150000`, AGENTS.md-locked); any shell file beyond the
  Payers section of `Sidebar.tsx` (no AppShell/PageHeader/layout or
  `src/components/ui/*` edits); and no new roles or capability system
  (TE-2 stands). No migration is needed for the nav change — a PR that
  ships one is out of scope. Resolver, generation, and extension
  contracts are unaffected by presentation-layer consolidation.

### Reviewer redlines (2026-07-16)

- **should-fix** — `docs/redesign/ROADMAP-STATUS.md` (E4.2 row) still says
  "📋 draft" while this epic is `reviewed: true` and the E4.2
  implementation is merged on `redesign` (PRs #170/#171; migrations
  `20260715140000`–`140600`, `20260716120000`, `20260716190000`,
  `20260716191000`). Stale program status; that file is author/PM-owned,
  so recorded here rather than edited.
- **nit** — TE-5 (2026-07-14) specified per-org seeded defaults including
  the organization-bootstrap RPC path; E4.0 built the vocabulary as
  `org_id NULL` **global** defaults instead (six seeded once,
  `UNIQUE NULLS NOT DISTINCT (org_id, code)`, migration
  `20260715120200`; F4.2.3 CRUD rides it in
  `src/services/denialReasonCodes.ts`). F4.2.3's acceptance criteria are
  met by the built shape (defaults non-deletable; org codes deactivate,
  never delete) — read TE-5's seeding mechanism as superseded by the
  built design, per `docs/data-model/table-register.md`.
- **nit** — the epic carries no explicit tables-read/tables-written trace
  (AGENTS.md requires one; REVIEW-HANDOFF checklist item 7). Reconciled
  during this review against `docs/data-model/table-register.md`: all
  E4.2 tables/columns (`denial_reason_codes` consumption,
  `next_best_action_configs`, `org_payer_settings`, SOP draft/execution-
  type/required-attrs columns, generation release scope, fill test
  markers) are registered. Future epics should carry the trace inline.
- **should-fix** (implementing-PR obligation, folded into TE-20d) —
  `e2e/sidebar-ia.spec.ts` and `e2e/payer-admin-module.spec.ts` pin the
  superseded Payers nav; they must change in the same PR as the TE-18
  sidebar edit, never after it.
