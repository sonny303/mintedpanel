---
epic: e0.7
title: Stage 0 Hardening — Public Surfaces, RLS Audit & Regression Coverage
stage: 0
status: reviewed # reviewer-managed process metadata only
owner: devin # PM-directed: drafted by the reviewing agent at the PM's request (2026-07-09)
reviewed: true # reviewed & technically enabled 2026-07-09; F0.7.1 mechanism gated on open BD-1, F0.7.5 a11y scope on open BD-2 (see Devin section + CLARIFICATIONS_NEEDED.md)
---

# E0.7 — Stage 0 Hardening — Public Surfaces, RLS Audit & Regression Coverage

## Purpose and Context

Stage 0 (E0.0–E0.6) shipped the redesigned shell, the org/party model, and —
for the first time — three **public, unauthenticated surfaces** that cross the
app's trust boundary: `/capture/:token` (E0.5), `/contact` (E0.5), and
`/share/:token` (E0.6), backed by `anon` SECURITY DEFINER RPCs. E0.7 hardens
what shipped before the PM promotes `redesign` → `main`: abuse resistance on
the public RPCs, a systematic RLS/GRANT audit of every table added in Stage 0,
type-system hygiene (hand-added `types.ts` entries), and regression e2e
coverage for gaps found during Stage 0 verification (e.g. the E0.6 TE-8
add-organization entry shipped with no spec). No new user-facing features; no
schema restructuring — additive migrations only, per AGENTS.md.

## Personas Empowered

- P1 Credentialing Manager (Sowmya Seed) — trusts that links she issues can't
  be brute-forced or replayed, and that revocation actually locks recipients out.
- P5 Practice Owner / P6 Office Manager — capture/share links keep working
  exactly as promised; no behavior change.
- (Indirectly) the business — the public surfaces cannot be used to enumerate
  tokens, spam leads, or probe org data.

## Component & Data Constraints

- Must be built from the current component library — see
  [uiux-component-guide.md](./uiux-component-guide.md) and [README.md](./README.md).
  E0.7 adds NO new UI surfaces; only existing error/lockdown states may be touched.
- Example/demo/test data must use scenario orgs and personas from
  [seed-universe.md](./seed-universe.md).
- All migrations are additive (AGENTS.md); no renames, no policy weakening.
- No new dependencies. Rate limiting must be implementable inside Postgres
  (the stack has no server middleware tier in Stage 0).

---

## Features

### F0.7.1 — Public RPC abuse resistance (rate limiting + uniform responses)

- **Description:** The four `anon`-callable RPCs (`validate_capture_token`,
  `submit_capture`, `submit_inbound_lead`, `validate_report_share`) gain
  DB-side attempt throttling and uniform failure responses so tokens cannot be
  enumerated and the lead queue cannot be spammed.
- **Persona:** P1 (protected); anonymous abuser (defeated).
- **Benefit hypothesis:** A 64-hex-char token is unguessable in theory, but
  unmetered validation endpoints invite scripted probing and lead spam;
  throttling closes the practical attack while keeping legitimate one-click
  usage untouched.
- **Acceptance criteria:**
  - Repeated failed validations from the same source are throttled after a
    threshold (BD-1 sets mechanism + numbers); a throttled caller receives the
    SAME generic "invalid or expired" response as a wrong token (no oracle).
  - Invalid, revoked, expired, and throttled outcomes are indistinguishable to
    an anonymous caller (existing lockdown pages render for all of them).
  - `submit_inbound_lead` enforces a per-source submission cap in addition to
    the existing honeypot; over-cap submissions are silently accepted-and-
    discarded or generically rejected (BD-1), never an error revealing the cap.
  - Legitimate flows (TS-7, TS-13, TS-14, TS-15) still pass unchanged.
  - No raw token is ever written to any log, table, or error message (audit of
    existing RPC bodies included).
- **Test/data scenario(s):** TS-16 (abuse probe), TS-7/TS-13/TS-14/TS-15
  (regression).

```gherkin
Feature: Public token endpoints resist enumeration
  Scenario: Scripted probing of capture tokens is throttled without an oracle
    Given an anonymous client has submitted 20 invalid tokens to validate_capture_token
    When it submits a 21st token, valid or not
    Then the response is the same generic invalid/expired result
    And a subsequently issued legitimate link for Lone Star Rehab Group still validates once the window passes
```

### F0.7.2 — Stage 0 RLS & GRANT audit (verified, not assumed)

- **Description:** A systematic, scripted audit of every table added in Stage 0
  (`parties`, `party_role_types`, `party_role_assignments`,
  `party_capture_links`, `inbound_leads`, `report_shares`) proving RLS is
  enabled, policies match the documented access model, GRANTs are explicit and
  minimal (AGENTS.md: every public table needs explicit GRANTs alongside RLS),
  and the `anon` role can reach ONLY the four intended RPCs.
- **Persona:** P1 and every org member (their data stays scoped).
- **Benefit hypothesis:** The access model was built piecewise across five
  epics; one adversarial pass catches any gap before promotion — cheaper now
  than as an incident later.
- **Acceptance criteria:**
  - An audit script (repo, re-runnable) asserts per table: RLS enabled,
    expected policy set, expected GRANTs, append-only where documented.
  - `anon` has EXECUTE on exactly the four public RPCs and nothing else; the
    SECURITY DEFINER helper functions from E0.2 remain client-revoked.
  - Cross-org checks: a member of org A cannot read org B's capture links,
    leads conversions, or shares via any browser path (TS-11 fixtures).
  - Findings are fixed by additive migration in the same PR, or explicitly
    recorded as accepted with rationale in this epic's Devin section.
- **Test/data scenario(s):** TS-9–TS-11 (party model), TS-16.

### F0.7.3 — Type-system & generated-artifact hygiene

- **Description:** Regenerate `src/integrations/supabase/types.ts` from the
  hosted schema so the hand-added `party_capture_links`, `inbound_leads`, and
  `report_shares` entries (added manually when MCP regen flaked in E0.5/E0.6)
  are normalized, and eliminate any drift between repo migrations and hosted DB.
- **Persona:** Developer agents (Claude Code/Devin) — indirect user benefit via
  fewer type-mismatch bugs.
- **Benefit hypothesis:** Hand-maintained generated files silently drift; one
  regen now prevents a class of future bugs.
- **Acceptance criteria:**
  - `types.ts` is regenerated (not hand-edited); `npx tsc --noEmit` stays clean.
  - A migration-vs-hosted diff shows zero drift, or drift is resolved by
    additive migration.
- **Test/data scenario(s):** gates only (tsc/lint/tests) — no seed scenario.

### F0.7.4 — Regression e2e coverage for Stage 0 gaps

- **Description:** Close the known Playwright coverage gaps: the E0.6 TE-8
  "Add organization" switcher entry (shipped with manual verification only),
  the E0.1 duplicate-name hard block, the E0.2/E0.3 only-sales-rep removal
  guard, and the E0.4 landing-resolver matrix (last-active / most-recent-live /
  first-run / all-inactive), all via the existing mock harness.
- **Persona:** P1 (regressions caught before they reach her).
- **Benefit hypothesis:** These flows are exactly the ones that broke or nearly
  broke during Stage 0 (the add-org entry point regression was user-reported);
  encoding them keeps them from regressing again.
- **Acceptance criteria:**
  - New specs under `e2e/` cover: switcher → Add organization → create → org
    switch (TS-4 fixtures); duplicate-name rejection (TS-6, exact
    normalized-name collision); only-sales-rep removal block (TS-9); landing
    resolver states (TS-0, TS-3, TS-12).
  - All specs run in the existing `Playwright smoke` CI job with no new infra.
- **Test/data scenario(s):** TS-0, TS-3, TS-4, TS-6, TS-9, TS-12.

### F0.7.5 — Public-surface UX lockdown consistency

- **Description:** One pass over the public routes (`/capture/:token`,
  `/contact`, `/share/:token`) verifying every terminal state (used, expired,
  revoked, invalid, throttled, submitted) renders the shared lockdown/empty
  pattern — no dead ends (E0.4's locked rule), no raw error text, pending/
  disabled states on all submit buttons.
- **Persona:** P5/P6 recipients and inbound strangers.
- **Benefit hypothesis:** These are the only pages outsiders ever see; a
  broken or leaking terminal state costs disproportionate trust.
- **Acceptance criteria:**
  - Every terminal state on the three routes renders a designed state with a
    path forward (matching existing lockdown components); verified in the e2e
    specs where mockable.
  - No server error shape (Postgres/PostgREST message) is ever rendered
    verbatim on a public route.
- **Test/data scenario(s):** TS-13, TS-14, TS-15, TS-16.

---

## Scenario/Seed Data Mapping

| Test scenario   | Org(s)/User(s)                                          | Purpose                                 |
| --------------- | ------------------------------------------------------- | --------------------------------------- |
| TS-16           | Anonymous (no fixture user); Lone Star Rehab Group link | Public-surface abuse probe + throttling |
| TS-7 / TS-13    | Lone Star Rehab Group; Rose City Rehab Collective       | Capture-link regression after hardening |
| TS-14 / TS-15   | Portfolio shares (P4 email; Rose City single-org)       | Share lockdowns incl. throttled state   |
| TS-0/3/4/6/9/12 | Per existing definitions                                | Regression e2e matrix (F0.7.4)          |

## Dependencies

- Everything E0.0–E0.6 merged on `redesign` (it is).
- Hosted-DB access for the RLS audit + types regen (same access used to apply
  Stage 0 migrations).
- BD-1 decision (rate-limit mechanism) before F0.7.1 builds.

## Out of Scope / Non-Goals

- No new features, routes, or nav changes; no component-library additions.
- No real email sending (E0.5 BD-2 stands), no CAPTCHA, no third-party
  abuse-prevention service (would violate no-new-deps).
- No auth changes for authenticated users; no `redesign` → `main` promotion.
- No accessibility overhaul (see BD-2 below — scoped keyboard/label pass only
  if the PM opts in).
- No performance work beyond what the audit surfaces as trivially fixable.

## 5. Technical Considerations & Enablers — Devin section

This section is owned by Devin.

> **Scope reminder.** E0.7 adds NO new user-facing features, routes, or
> components; every enabler below is additive-only (per AGENTS.md) and lands as
> a re-runnable audit, an additive migration, a generated-artifact regen, or an
> e2e spec. The whole epic verifies and closes gaps in what E0.0–E0.6 already
> shipped before the PM promotes `redesign` → `main`.

### Current-state findings (verified against the codebase, 2026-07-09)

- **The four `anon` RPCs exist exactly as the epic names them.** All are
  `SECURITY DEFINER` with `SET search_path TO 'public'` and
  `GRANT EXECUTE … TO anon`: `validate_capture_token(text)` and
  `submit_capture(text, jsonb)` (in `20260709140000_party_capture_links.sql`),
  `submit_inbound_lead(jsonb)` (`20260709140100_inbound_leads.sql`), and
  `validate_report_share(text)` (`20260709150000_report_shares.sql`). The
  operator-side RPCs (`create_capture_link`, `create_report_share`,
  `revoke_report_share`) are `REVOKE ALL … FROM public` + granted to
  `authenticated` only — anon cannot reach them.
- **No throttling exists today.** There is no attempts/rate-limit table and no
  per-source counter anywhere in `supabase/migrations/`. `submit_inbound_lead`
  has only the honeypot (`company_website`) + required-field validation; the
  three token RPCs have no abuse control beyond token entropy. F0.7.1 is net-new
  and blocked on **BD-1** (mechanism + thresholds — a PM business decision).
- **No raw token is persisted or logged.** Every RPC stores only
  `token_hash` (`encode(sha256(convert_to(token,'UTF8')),'hex')`); the raw
  token lives only in the URL returned once by the issue RPC. `audit_log`
  descriptions carry the recipient email + org name, never a token. F0.7.1's
  "no token in any log/table/error" audit confirms this holds and adds a
  regression assertion.
- **E0.2 helpers are client-revoked.** `assert_contact_valid(jsonb,text)` and
  `insert_contact_party(jsonb,uuid)` are `REVOKE ALL … FROM public` (called only
  through the owning definer RPCs via ownership chaining). Note they are plain
  `SECURITY INVOKER` functions, not `SECURITY DEFINER` — F0.7.2's audit should
  assert "not `anon`-executable" rather than assume a definer flag.
- **`types.ts` drift confirmed.** `inbound_leads`, `party_capture_links`, and
  `report_shares` are present in `src/integrations/supabase/types.ts` but were
  hand-added when MCP regen flaked (E0.5/E0.6). F0.7.3 regenerates the file.
- **e2e harness is mock-based.** Sandbox/CI cannot reach `*.supabase.co`, so
  specs mock `/rpc/<name>` via Playwright `context.route` (see
  `e2e/capture-link.spec.ts`). Existing coverage: `capture-link`,
  `contact-inbound`, `reporting-center`, `report-share`,
  `portfolio-inactive-fallback`. The F0.7.4 gaps (add-org switcher,
  duplicate-name block, only-sales-rep removal, landing resolver matrix) have
  **no** spec today.

### Open PM decisions (business decisions — NOT reviewer-resolvable)

Both are logged in the root `CLARIFICATIONS_NEEDED.md` and remain the PM's call;
the reviewer's recommendations (below and in that file) are technical input, not
a resolution. `reviewed: true` signs off the epic's grammar/enablers; it does
**not** lift these build gates — Claude Code must honor them per Dependencies.

- **BD-1 — rate-limit mechanism + thresholds (gates F0.7.1 build).** Reviewer
  recommends option (a): a small additive `public_rpc_attempts` table keyed by a
  coarse source fingerprint (RPC name + a hashed caller hint), checked/incremented
  inside each `anon` RPC, pruned lazily; over-threshold returns the SAME generic
  invalid/expired response (no oracle). This fits the no-new-deps / no-middleware
  Stage 0 posture. Final numbers (e.g. 20 failed validations / 15 min; 5 inbound
  leads / hour) are the PM's to set.
- **BD-2 — scoped a11y pass on the public routes (gates F0.7.5 scope only).**
  Reviewer recommends option (a): a scoped labels/focus-order/keyboard-submit/
  error-announcement pass on `/capture`, `/contact`, `/share` only. If the PM
  defers, F0.7.5 ships the terminal-state/lockdown-consistency audit without the
  a11y sweep.

### Technical enablers

- **TE-1 — Public-RPC throttling (F0.7.1), additive, pending BD-1.** New
  additive migration adding a `public_rpc_attempts` counter table (no PII: RPC
  name + hashed source hint + window bucket + count) and folding a
  check-and-increment into all four `anon` RPCs via `CREATE OR REPLACE`
  (additive re-definition — no signature/column change). Over-threshold paths
  return the existing generic shapes: `{state:'invalid'}` for the token RPCs,
  `{ok:true}` (accept-and-discard) for `submit_inbound_lead`, so throttled ≡
  invalid/expired to an anon caller (no oracle). Honeypot behavior is retained.
  Numbers/mechanism finalized by BD-1.
- **TE-2 — Uniform-response + no-token-logging audit (F0.7.1).** A re-runnable
  check (script or SQL test) asserting invalid/revoked/expired/throttled outcomes
  are byte-identical to an anon caller, and grepping the RPC bodies + `audit_log`
  usage to prove no raw token is ever written. This half of F0.7.1 does NOT
  depend on BD-1 and can land first.
- **TE-3 — Stage 0 RLS & GRANT audit (F0.7.2), verified not assumed.** A
  re-runnable audit (checked into the repo, e.g. `scripts/` + a SQL/test
  harness) asserting, per Stage 0 table — `parties`, `party_role_types`,
  `party_role_assignments` (E0.1), `party_capture_links`, `inbound_leads`
  (E0.5), `report_shares` (E0.6): (a) `relrowsecurity` is on; (b) the expected
  policy set exists (org-scoped `user_org_ids()` for `party_capture_links` and
  `party_role_assignments`; `created_by = auth.uid()` for `report_shares`; the
  shared-queue `true` policies for `inbound_leads`; the assignment-membership-OR-
  `created_by` policy for `parties`); (c) GRANTs are explicit and minimal; (d)
  `anon` holds EXECUTE on EXACTLY the four public RPCs and nothing else, and
  `user_org_ids()` + the E0.2 contact helpers stay non-`anon`. Cross-org reads
  (org A cannot see org B's links/leads-conversions/shares) are exercised via
  the TS-11 fixtures. Findings are fixed by additive migration in the same PR or
  recorded as accepted-with-rationale here.
- **TE-4 — `types.ts` regen + migration-vs-hosted drift check (F0.7.3).**
  Regenerate `src/integrations/supabase/types.ts` from the hosted schema
  (Supabase type generation) so the three hand-added tables are normalized;
  `npx tsc --noEmit` must stay clean. Diff repo migrations against the hosted
  DB; resolve any drift by an additive migration (never by editing an existing
  one). No hand-edits to the generated file.
- **TE-5 — Regression e2e specs (F0.7.4), existing mock harness only.** New
  specs under `e2e/` on the `context.route` `/rpc/<name>` pattern: (1) org
  switcher → **Add organization** → create → org switch (E0.6 TE-8 gap; TS-4
  fixtures); (2) duplicate normalized-name hard block (E0.1 F0.1.4; TS-6 exact
  collision); (3) only-sales-rep removal guard (E0.2/E0.3; TS-9); (4) landing
  resolver matrix — last-active / most-recent-live / first-run / all-inactive
  (E0.4 `resolveLanding`/`selectActiveOrgId`; TS-0/TS-3/TS-12). All run in the
  existing `Playwright smoke` CI job — no new infra, no new deps.
- **TE-6 — Abuse-probe e2e (F0.7.1/F0.7.5, TS-16).** A mock-harness spec that
  drives repeated invalid validations and burst lead submissions and asserts the
  uniform response + that a legitimate control link still validates. Full DB-level
  throttle numbers are verified in the SQL/audit harness (TE-1/TE-2), since the
  mock harness cannot exercise the real counter.
- **TE-7 — Public-route terminal-state consistency (F0.7.5).** An audit pass +
  spec coverage over `/capture/:token`, `/contact`, `/share/:token` confirming
  every terminal state (used / expired / revoked / invalid / throttled /
  submitted) renders the shared lockdown/empty pattern with a path forward and
  never surfaces a raw Postgres/PostgREST error shape. Uses existing empty/
  lockdown components only. The a11y sub-scope is gated on BD-2.

### Tech-debt findings

- **TD-1 — Lazy expiry has no sweeper (carried from E0.5 TD-3 / E0.6 TD-3).**
  `party_capture_links` and `report_shares` flip to `expired` only when a token
  is next touched; stored `state` can lag reality. Correct for callers, but the
  RLS/GRANT audit and any throttle counters should not assume `state` is
  eventually-consistent without a touch. A sweep job stays future work; note it,
  do not build it in Stage 0.
- **TD-2 — Coarse fingerprinting is best-effort.** With no middleware tier, the
  BD-1 counter can only key off what an `anon` RPC sees (RPC name + a hashed
  hint); it deters scripted probing but is not a true per-client limit. Document
  this as an accepted Stage 0 limitation, not a guarantee.
- **TD-3 — `inbound_leads` shared-queue RLS is intentionally broad.** Its
  SELECT/UPDATE policies are `USING (true)` for all `authenticated` users (no
  `org_id` — leads pre-date any org, E0.5 TD-6). The F0.7.2 audit must record
  this as an accepted exception to the org-scoped convention, not flag it as a
  gap.
- **TD-4 — Orphan ad-hoc parties.** `create_capture_link` may provision a
  person party that is never used if the link is re-issued/never submitted (E0.5
  TD-4); no deletion in Stage 0. Out of scope to clean up here; the RLS audit
  should confirm such rows are still access-scoped.
- **TD-5 — `types.ts` will re-drift** if a future migration lands without a
  regen. F0.7.3 fixes the current drift but does not prevent recurrence; a regen
  step in the build checklist is the durable fix (note for the PM/pipeline, not
  built here).

### Additive schema needs (describe only — no migrations written here)

- `public_rpc_attempts` (or equivalent) for BD-1 throttling — additive table +
  `CREATE OR REPLACE` of the four `anon` RPCs. Only built once BD-1 picks
  option (a).
- Any RLS/GRANT gap TE-3 surfaces is closed by a new additive migration in the
  same PR (e.g. a missing explicit GRANT or a policy tightening) — never by
  editing an existing migration, never by weakening a policy.

### Seed / test data

No new baseline fixtures. TS-16 (defined in `seed-universe.md`) is exercised
entirely through the mock/e2e harness plus the DB-level throttle/RLS tests;
TS-0/3/4/6/9/11/12 reuse existing fixtures/scenario mutations. All demo data
stays within the seed universe (`seed-redesign.sql`); nothing seeds production.

### Sign-off status

**Reviewed, technically enabled, and signed off (`reviewed: true`).** The epic
is internally consistent, references only defined artifacts (TS-16 and all cited
TS IDs exist in `seed-universe.md`), and its enablers are fully specified above.
Two **open PM business decisions** remain and are preserved as explicit build
gates (they are not review blockers and not reviewer-resolvable): **BD-1** gates
the F0.7.1 throttle mechanism/thresholds, and **BD-2** gates F0.7.5's a11y
sub-scope. Claude Code may build the un-gated work now — F0.7.2 (RLS/GRANT
audit), F0.7.3 (types regen + drift check), F0.7.4 (regression e2e), F0.7.1's
uniform-response/no-token-logging audit half (TE-2), and F0.7.5's terminal-state
audit — and must hold the BD-gated pieces until the PM resolves BD-1/BD-2.
PR contract: one PR titled `E0.7: Stage 0 Hardening — Public Surfaces, RLS Audit
& Regression Coverage` targeting `redesign`, covering F0.7.1–F0.7.5 / TE-1–TE-7,
every FR traceable in the diff. Do NOT promote `redesign` → `main` (PM's call).

## Revision History

- 2026-07-09, Devin (at PM request), initial draft — hardening scope for
  Stage 0 close-out, pending PM decisions BD-1/BD-2.
- 2026-07-09, Devin (reviewer): reviewed grammar/formatting (no scope changes)
  and appended the Devin section with current-state codebase findings,
  enablers TE-1–TE-7, tech-debt TD-1–TD-5, and additive schema needs.
  **Signed off `reviewed: true`.** BD-1 (throttle mechanism) and BD-2 (public-
  route a11y scope) stay OPEN in `CLARIFICATIONS_NEEDED.md` as explicit build
  gates on F0.7.1 and F0.7.5 respectively — not review blockers; the rest of
  the epic is buildable now.
