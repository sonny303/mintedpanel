# E6.11 payer PDF field mapping — handoff

Companion to [`E6.11-payer-pdf-field-mapping.md`](E6.11-payer-pdf-field-mapping.md)
(the epic: validated spike findings, corrected premises, bite list). This file
is the state of the build and what the next session does.

Branch `devin/1787617390-e611-payer-pdf-mapping-epic`, PR
[#341](https://github.com/sonny303/mintedpanel/pull/341). Continued on
`claude/payer-pdf-field-mapping-handoff-e9sr4z` (merged Devin's four commits,
then applied the migration and closed one of the two next-step items below).

## What works end to end

Code only — exercised against the hosted schema, but no browser walkthrough
with a real payer PDF yet (see Next steps).

1. **Setup** — a payer/SOP template with a payer form attached shows a
   collapsed **Field mapping** panel (`PayerFormFieldPanel`) under the form.
2. **Import** — "Import fields from the PDF" signs a download for the exact
   attached form row, reads its AcroForm fields in the browser, and proposes one
   shared registry row per fillable control, keyed
   `payer-form:<payer_forms.family_id>`. A flat scan says so explicitly instead
   of importing nothing silently.
3. **Train** — the same `FieldRegistryList` the online-form trainer uses: token,
   fixed value, "a person writes this", transform, row/section renaming, and the
   mapped/to-decide coverage readout.
4. **Sample fill** — "Download a sample fill" fills the real blank with the
   synthetic mock profile, labels the file `SAMPLE-do-not-send`, and records a
   `fill_sessions` row with `is_test=true, fill_mode='pdf'`.
5. **Case fill** — the case checklist's payer-form row gains **Fill & download**
   when the family has mappings that resolve. It fills the baked form version,
   downloads locally, leaves the PDF editable, records
   `fill_sessions(fill_mode='pdf', is_test=false)` counts-only, and does not
   touch task or case status.

## Decisions worth keeping

- Mappings are keyed by **family**, not by form row, so replacing a payer's PDF
  keeps the training. A payer that reorganizes its layout should be uploaded as
  a new family.
- `planPayerFormFill` delegates to `classifyFieldMap` — hardcoded rows fill, a
  "person writes this" row is reported as theirs and **not** as a coverage gap,
  and an undecided row is a gap that is never guessed at.
- The filled file never leaves the browser: the `payer-forms` bucket holds blank
  global forms only, and no field value is written to a row, an audit entry, or
  a log.
- The old dictionary-backed filler (`pdfFill.ts` + `field_dictionary`) is still
  in the tree. Its own matching is provably dead (see B1 below), but — contrary
  to what this doc claimed before — a route DOES still import it: it is the
  "pdf" step type's upload-and-fill affordance in `StepDetails.tsx`
  (`src/routes/cases.$id.tsx` → `CaseTasksPanel` → `StepDetails`), unrelated to
  Payer PDF. Nothing NEW in E6.11 depends on it.

## Next steps, in order

1. ~~**Apply the migration to hosted.**~~ **Done 2026-08-25.** Applied
   `supabase/migrations/20260825120000_e611_pdf_field_maps.sql` via Supabase
   MCP (project `fkvuhfsqcmujywzgczmc`) and object-verified: hosted
   `propose_shared_field_map` is now the 10-arg signature (`p_map_type text
DEFAULT 'web'`), grants are `authenticated, service_role` only (no `anon`,
   no stray old overload). Rollback-wrapped probes as a real member confirmed:
   a PDF row inserts with `map_type='pdf'`; a web caller omitting `p_map_type`
   still gets `'web'` (no extension regression); re-proposing an existing PDF
   row with `p_map_type='web'` does NOT flip its tier (identity-lock holds);
   an invalid `map_type` is rejected. Regenerated `types.ts` from hosted and
   diffed against the committed file — **byte-identical after prettier**, so
   Devin's hand-narrowing was exactly right and needed no changes.
   `docs/data-model/table-register.md` and `CLAUDE.md`'s migration-status list
   are updated to match.
2. **Walk the real flow on hosted** with a genuine payer PDF: import → decide
   every field → sample fill → generate a case → Fill & download. **Still
   open** — this sandbox has no real payer PDF and cannot reach the hosted
   Storage bucket to exercise the browser flow end to end; needs a person (or
   a session with a real file + UAT org access) to run it.
3. **B0, now answerable with real files** — how many payer PDFs in the library
   are AcroForm vs flat? **Still open**, blocked on the same missing files as
   step 2.
4. ~~**B1** — retire the inert dictionary filler.~~ **Checked, NOT done — the
   stated precondition doesn't hold.** The plan was "safe once no route
   imports `pdfFill.ts`/`pdfFillClient.ts`"; in fact `StepDetails.tsx` does,
   for the generic "pdf" task-step upload button, which is live UI, not dead
   code. What IS true: `mapPdfFields` only matches `field_dictionary` rows
   with `status='confirmed'`, and the only writer of that status
   (`decideDictionaryEntry`, the "Fix-it" confirm card) is called from
   nowhere in `src/` — so the feature is reachable but its matching can never
   produce a hit; every upload reports every field unmapped. Deleting it now
   would remove a live (if silently-useless) upload/fill button from PDF-type
   task steps, which is a product call, not a safe cleanup — flagging for a
   decision: either build the missing confirm step, or deliberately retire
   the step type and its UI together. `upsertDictionaryEntry` (used by the
   UNRELATED online-form trainer's "learned suggestion" feature, S5.3) is
   fine and must stay.
5. ~~**Coverage gaps to close**~~ **Done 2026-08-25.** Split
   `fillAndDownloadPayerForm` into a pdf-lib-only `fillPayerFormBytes` (no
   DOM) plus a thin browser wrapper that only adds the download trigger, and
   added `src/lib/payerFormFillClient.test.ts` — a real generated AcroForm
   fixture (text/dropdown/radio/checkbox/pushbutton fields) pinning exact and
   case/space-insensitive option matching, checkbox coercion across the
   `CHECKED` vocabulary, the pushbutton/unsupported-field rejection path, a
   missing-selector rejection that doesn't abort the rest of the fill, and
   that an unmentioned field is left untouched. 13 new tests, all passing.
6. **Deferred deliberately:** roster-grain fills (one PDF, many providers),
   multi-page repeated field groups, and any transform beyond the two authorable
   ones. `applyRegistryTransform`'s `state_abbrev` normalizes case only — this
   repo has no state name→code table, and inventing one duplicates the
   extension's.

## Verified this session (2026-08-25, after merging Devin's branch + the above)

`npm run lint` (0 errors, 14 pre-existing warnings), `npx tsc --noEmit`, and
`npm run test` (149 files, 1963 tests) all pass. `npm run test:e2e` and a
browser walkthrough against a real payer PDF are **still not done** — see
step 2.
