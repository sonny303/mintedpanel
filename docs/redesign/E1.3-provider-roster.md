---
epic: e1.3
title: Provider Roster
stage: 1
release: R1
status: reviewed # reviewer-managed process metadata only
owner: devin
reviewed: true # reviewed & technically enabled 2026-07-11 (see Devin section §5; note TE-1: providers.gender already exists — no migration for it)
---

# E1.3 — Provider Roster

## Purpose and Context

The roster is the people leg of the scope: the clinicians who will be
credentialed. E1.3 delivers the "Providers" wizard section (E1.0 framework)
capturing the **CAQH-anchored provider baseline** (PM, 2026-07-10: "baseline =
what CAQH requires," minus full SSN, work history, and disclosure questions —
those live in CAQH itself, proxied by a current attestation). E1.3 also lands
the two R1 schema decisions locked on 2026-07-10: the **M:N provider↔group
model** (`provider_group_assignments` join table — a provider always belongs
to at least one group; no unassigned parking lot) and **license primary-source
verification (PSV)** — the team verifies each license against the state board
website (e.g. KS, NC lookups), and that verification must be recorded, not
lost. Licenses are hand-entered in v1 (locked); CAQH/NPPES import is R5.
This epic's join table is the prerequisite for the 4-part case key
(`provider × group × payer × state`) — the AGENTS.md 3-part case-key rule is
amended by the epic that implements generation (E2.3), not here.

## Personas Empowered

- **P1 Credentialing Manager (Sowmya)** — sole actor; enters providers from
  client intake documents and CAQH profiles. Internal-only wizard through
  Stage 3 (locked).
- **P7 Provider** — NOT an actor in R1 (reserved persona); their data is
  captured about them, not by them.

## Component & Data Constraints

- Built from the current component library — see
  [uiux-component-guide.md](./uiux-component-guide.md) and
  [README.md](./README.md).
- Test data uses [seed-universe.md](./seed-universe.md) orgs.
- Implementation dependencies (explicit):
  - `providers` table exists with name, credentials, DOB, `ssn_last4`,
    contact + home address, `npi`, `caqh_id`, `caqh_last_attested_date`,
    taxonomy/specialty (PT defaults), start date, education (degree, school,
    graduation date), malpractice (carrier, policy, coverage dates), status
    (default `onboarding`).
  - `state_licenses` table exists (provider FK, state, license_number,
    license_type full|compact, issue/expiration dates, status; unique on
    provider+state+number) — multiple licenses per provider supported today.
  - **New schema (additive migrations in this epic):**
    `provider_group_assignments` (org_id, provider_id, group_id, is_primary,
    start/end dates; unique provider+group) and PSV columns on
    `state_licenses` (`verified_status`, `verified_at`, `verified_by`,
    `verification_source_url`). `providers.gender` added (CAQH-required,
    missing today). Table-register rows updated in the same PR.
  - `providers.group_id` becomes a frozen primary-group mirror (locked
    decision); new reads go through the join table.
- Downstream field usage: NPI/license/CAQH attestation → E1.8 readiness and
  R9 Expiration Radar clocks; group assignments → 4-part case key (R4);
  malpractice dates → readiness "COI current"; PSV trail → readiness
  "license verified".

---

## Features

### F1.3.1 — Provider form (CAQH-anchored baseline)

- **Description:** The Providers section lists the org's roster and opens a
  form capturing: first/last name, credentials, gender, DOB, SSN last-4,
  email/phone, home address, Type 1 NPI, taxonomy code + specialty
  (PT-defaulted), start date, education (degree/school/graduation),
  malpractice block (carrier, policy number, coverage start/end), CAQH ID +
  last attestation date, and group assignment(s) (F1.3.2).
- **Persona:** P1 Credentialing Manager.
- **Benefit hypothesis:** An application-ready provider profile is captured
  once at the CAQH grain, so downstream payer applications and readiness
  checks never require a data round-trip back to the client.
- **Acceptance criteria:**
  - Required to save: name, Type 1 NPI (10 digits, format-level validation
    only in v1), and ≥1 group assignment. All other CAQH-baseline fields are
    on the form as optional — absence never blocks save (readiness, not
    entry, is the gate: E1.8).
  - SSN captured as **last-4 only** (hard rule; full-SSN storage is the
    future Sensitive Identifiers Vault epic, R6/R7 — PM, 2026-07-10).
  - Work history and disclosure questions are NOT on the form; "CAQH
    attestation current" is the proxy (PM, 2026-07-10).
  - Provider status stays hidden; new providers default to `onboarding`
    (PM, 2026-07-10 — no status picker in R1).
  - List view: name, credentials, NPI, groups, license states, CAQH
    attestation date; edit from the list; soft delete via terminated status,
    never a row delete.
  - Component → Hook → Service → Supabase; `org_id` from the active org.
  - Derived progress: ≥1 provider → section Complete (E1.0 F1.0.2).
- **Test/data scenario(s):** TS-33 (new — see seed additions).

```gherkin
Feature: Provider capture
  Scenario: P1 adds Tree Hill's first clinician
    Given Tree Hill Sports Therapy has a saved group and facility
    When P1 adds a provider with name, NPI, CAQH ID, and assigns the group
    Then a providers row and a provider_group_assignments row exist
    And the Providers section shows Complete
    And the wizard shows the all-complete state with future-section preview
```

### F1.3.2 — Group assignment (M:N, required at entry)

- **Description:** Group selection is step one of the add-provider form: a
  required picker over the org's E1.1 groups, multi-select (M:N from day
  one), with one group marked primary. No provider exists unassigned
  (PM, 2026-07-10: "we should always have a group").
- **Persona:** P1 Credentialing Manager.
- **Benefit hypothesis:** The case key's group leg is populated at entry, so
  R4 generation never encounters an orphan provider; multi-TIN clinicians
  are representable the day they appear.
- **Acceptance criteria:**
  - Required multi-select of active groups; exactly one marked `is_primary`.
  - Writes `provider_group_assignments` rows; `providers.group_id` mirrors
    the primary (frozen legacy mirror, no new readers).
  - Assignments editable after creation (add/remove group, change primary);
    removing the last assignment is blocked.
- **Test/data scenario(s):** TS-34 (new — multi-group provider on Shelby
  Sports Rehab, mirroring the PM's two-TIN therapist example).

```gherkin
Feature: Multi-group provider
  Scenario: The same therapist works under two TINs
    Given Shelby Sports Rehab has two provider groups
    When P1 adds a provider assigned to both groups with the first as primary
    Then two provider_group_assignments rows exist for the provider
    And the roster list shows both groups on the provider row
```

### F1.3.3 — State licenses with PSV trail

- **Description:** Each provider carries one or more state licenses
  (number, state, type full/compact, issue/expiration). Each license row
  records its **primary-source verification**: verified status, date,
  verifier, and the state-board lookup URL used — capturing the team's real
  workflow of confirming the license on the state website (KS, NC, etc.).
- **Persona:** P1 Credentialing Manager.
- **Benefit hypothesis:** The one-time-per-cycle verification is recorded
  instead of living in someone's memory; readiness (E1.8) can require
  "license verified," and renewals re-trigger verification (R9 clocks).
- **Acceptance criteria:**
  - Multiple licenses per provider supported in the form (locked — test
    cases exist).
  - License fields map to `state_licenses`; PSV fields are the new additive
    columns (`verified_status unverified|verified|failed`, `verified_at`,
    `verified_by`, `verification_source_url`).
  - Marking verified requires the lookup URL; the verifier and timestamp are
    set automatically.
  - **Re-verification at renewal** (PM, 2026-07-10): editing a license's
    expiration date resets `verified_status` to `unverified` — feeds the R9
    Expiration Radar re-verify workflow later.
  - Expiration date drives the roster list's license summary (soonest
    expiry shown).
- **Test/data scenario(s):** TS-35 (new — license entry + PSV + renewal
  reset).

```gherkin
Feature: License primary-source verification
  Scenario: P1 verifies an NC license against the state board
    Given a provider with an NC license marked unverified
    When P1 records the NC board lookup URL and marks it verified
    Then the license shows verified with P1 and today's date
    When P1 later updates the license expiration date after renewal
    Then the license returns to unverified
```

### F1.3.4 — Roster summary on Account Detail

- **Description:** Account Detail gains a read-only roster summary (count,
  names, license states), consistent with the E1.1/E1.2 summaries; editing
  stays in the wizard.
- **Persona:** P1 Credentialing Manager.
- **Benefit hypothesis:** One glanceable client record; single editing
  surface preserved.
- **Acceptance criteria:**
  - Read-only list; empty state links to the wizard section.
- **Test/data scenario(s):** TS-33.

---

## Scenario/Seed Data Mapping

| Test scenario | Org(s)/User(s)               | Purpose                                          |
| ------------- | ---------------------------- | ------------------------------------------------ |
| TS-33 (new)   | Tree Hill Sports Therapy, P1 | First provider, CAQH baseline, all-complete flow |
| TS-34 (new)   | Shelby Sports Rehab, P1      | Multi-group (two-TIN) provider, M:N assignments  |
| TS-35 (new)   | Outer Banks Rehab Group, P1  | Multi-license provider, PSV trail, renewal reset |

Seed additions in the same delivery: seeded providers + licenses on Outer
Banks Rehab Group (incl. one verified NC license) and TS-33–TS-35 rows in
`seed-universe.md`.

---

## Dependencies

- E1.0 framework + derived progress; E1.1 groups (assignment picker).
- Additive migrations: `provider_group_assignments`, PSV columns on
  `state_licenses`, `providers.gender`; table-register updates in the same
  PR.
- Table trace: reads/writes `providers`, `state_licenses`,
  `provider_group_assignments` (new); reads `provider_groups`,
  `organizations`.

---

## Out of Scope / Non-Goals

- No full-SSN storage (Sensitive Identifiers Vault epic, R6/R7 — encrypted
  vault, role-gated reveal/fill, access audit).
- No work-history or disclosure-question capture (CAQH holds them; R5 import
  may pull them).
- No CAQH/NPPES automated import or NPI registry validation (R5).
- No bulk roster import (E1.3b, R5).
- No provider↔facility assignment (E1.4, R3).
- No document uploads (COI, diplomas) — future.
- No status picker or status workflow (default `onboarding`).
- No AGENTS.md case-key amendment here (lands with E2.3 generation dedup).

---

## Revision History

- 2026-07-10, Devin (in-session with PM), initial draft; CAQH-baseline field
  set, M:N-required-at-entry, PSV trail, and SSN last-4 lock per the
  2026-07-10 PM session.
- 2026-07-15, independent Claude Code review session (per the PM request and
  `docs/redesign/REVIEW-HANDOFF.md`), §5 amendment: added **TE-11** authorizing
  the `src/components/layout/Sidebar.tsx` edit that points the org-zone
  "Providers" nav item at `/onboarding/wizard?section=providers`, superseding
  only the Providers half of E0.9 F0.9.3 (Facilities stays reserved;
  `/providers` stays direct-URL-only). No other section changed; `reviewed: true`
  is unchanged (the flag remains the PM's call).

---

## 5. Technical Considerations & Enablers

_Reviewer-owned (Devin). Verified against the baseline schema
(`20260704210000_baseline_live_schema.sql`), the E0.10 constraint migrations,
the table register, and the existing provider/license services on 2026-07-11._

- **TE-1 — Schema audit: `providers.gender` ALREADY EXISTS — do not migrate
  it.** The baseline `public.providers` table already has a `gender text`
  column (alongside `ethnicity`, `middle_initial`, `suffix`, and the full CAQH
  baseline: `date_of_birth`, `ssn_last4`, contact + home address, `npi`,
  `caqh_id`, `caqh_last_attested_date`, `taxonomy_code` (PT default),
  `specialty` (PT default), `start_date`, `status` (default `onboarding`),
  `degree`/`school_name`/`graduation_date`,
  `malpractice_carrier`/`_policy_number`/`_coverage_start`/`_coverage_end`).
  The epic body's "`providers.gender` added (CAQH-required, missing today)"
  claim is inaccurate — gender is present, so **no column is added for it**.
  The provider baseline is entirely UI/service work with no provider-table
  migration. (Body text left unedited per the reviewer/author-section split;
  this note is the authoritative schema correction.)
- **TE-2 — The two genuinely additive schema needs.** Only two additive
  migrations are required, both endorsed by the current model:
  1. **`provider_group_assignments`** (M:N provider↔group join). The table
     register explicitly names this as planned and says
     `provider_facility_assignments` is its **template** — mirror that table's
     shape and policies: columns `org_id`, `provider_id`, `group_id`,
     `is_primary`, `start_date`, `end_date`; `UNIQUE (provider_id, group_id)`;
     FKs to `organizations`/`providers`/`provider_groups`; org-scoped RLS
     (member SELECT; specialist/admin INSERT/UPDATE/DELETE) and the standard
     `authenticated` grants. Add an index on `group_id` (the non-leading FK)
     per the E0.10 FK-index convention. Consider a partial unique index to
     enforce exactly one `is_primary` per provider (E0.10 enforced the same on
     `provider_facility_assignments`).
  2. **PSV columns on `state_licenses`** (additive): `verified_status`
     (`unverified|verified|failed`, default `unverified`), `verified_at`,
     `verified_by`, `verification_source_url`. `state_licenses` already exists
     with `provider_id` (E0.10 NOT-NULL), `state` (E0.10 `^[A-Z]{2}$`),
     `license_number`, `license_type` (CHECK `full|compact`), `issue_date`,
     `expiration_date`, `status`, and `UNIQUE (provider_id, state,
license_number)`.
     Update `docs/data-model/table-register.md` for both in the same PR (add the
     `provider_group_assignments` row; note the PSV columns on `state_licenses`).
- **TE-3 — Reuse the existing provider+license write path.**
  `src/services/providers.ts` already implements the DI-seam CRUD plus
  `createProviderWithDetails` (provider + `state_licenses` + facility
  assignments in one call) and `updateProviderWithLicenses` (diff-based
  insert/update/delete of license rows). E1.3 extends these: swap/augment the
  `facilityIds` bundle with **group assignments** (write
  `provider_group_assignments`, set `is_primary`) and thread the PSV fields
  through the license insert/update. `queryKeys.providers(orgId, filters)`,
  `queryKeys.provider(orgId, id)`, and `queryKeys.stateLicenses(orgId,
providerId)` are the cache keys; add a group-assignments key + shared
  invalidator. Keep the layering: component → `useProviders`/new hook →
  service → `externalClient`.
- **TE-4 — `providers.group_id` is a frozen legacy mirror.** The primary-group
  write continues to set `providers.group_id` (table register calls the M:N
  split "really M:N"; the column stays for legacy readers), but all new reads
  go through `provider_group_assignments`. Do not add new readers of
  `providers.group_id`. Removing a provider's last assignment is blocked
  (mirrors the E0.2/E0.3 "can't remove the only sales rep" pattern).
- **TE-5 — PSV trail and renewal reset are service-enforced.** Marking a
  license verified requires `verification_source_url`; set `verified_at =
now()` and `verified_by = currentUserId()` server-side (never client-
  supplied). Editing a license's `expiration_date` resets `verified_status` to
  `unverified` — implement in `updateProviderWithLicenses` (compare incoming
  vs stored expiration per matched row). NOTE: `state_licenses` is **not** an
  append-only spine (only `touches`/`status_history`/`audit_log` are), and the
  existing service already UPDATEs/DELETEs license rows under RLS — so the PSV
  reset is a legitimate UPDATE, not an append-only violation. Audit every PSV
  transition via the existing `writeAudit`.
- **TE-6 — PHI boundary on the list projection.** The roster list must keep
  using the PHI-safe `PROVIDER_LIST_COLUMNS` projection in `providers.ts`
  (no `ssn_last4`, `date_of_birth`, or home-address columns; `home_state`
  intentionally included for routing). Add `caqh_last_attested_date` (already
  in the projection) and derive license-state / soonest-expiry summaries from
  a scoped `state_licenses` read — do **not** widen the list to `select("*")`.
  SSN is captured/stored as `ssn_last4` only (full-SSN vault is R6/R7); the
  form must never accept a full SSN.
- **TE-7 — Form scope & defaults.** Required-to-save = name, Type 1 `npi`
  (10-digit format-level only, no registry check in v1), and ≥1 group
  assignment; all other CAQH-baseline fields are optional (readiness, not
  entry, is the gate — E1.8). No status picker (default `onboarding`); no
  work-history/disclosure capture (CAQH attestation is the proxy). Soft delete
  = set `status = 'terminated'` (`ProviderStatus` union already =
  `onboarding|active|terminated`; `terminateProvider` exists), never a row
  delete. NPI format validation belongs in a pure, unit-tested `src/lib/`
  helper.
- **TE-8 — Wizard integration.** Section renders inside E1.0's onboarding
  wizard route using the E1.0 section shell; feature component under
  `src/components/providers/` or `src/components/onboarding/`. Group picker
  sources the E1.1 groups. Derived progress (E1.0 F1.0.2): "≥1 `providers` row
  = Complete" — presence-based, no stored wizard flag. F1.3.4 Account-Detail
  roster summary reuses the list read, read-only.
- **TE-9 — Case-key amendment is explicitly NOT here.** The join table is the
  prerequisite for the 4-part case key (`provider × group × payer × state`),
  but the AGENTS.md rule "one credentialing case per `(provider_id, payer_id,
state)`" is amended by the generation epic (E2.3), not E1.3. E1.3 adds only
  the join table; it introduces no case-generation logic and no AGENTS.md
  edit. Reviewers should not treat the unchanged AGENTS.md rule as a
  contradiction for this epic.
- **TE-10 — Verification gates.** Unit-test the NPI validator, the primary-
  assignment invariant, the last-assignment-removal block, and the PSV
  renewal-reset logic; extend provider e2e for TS-33 (CAQH baseline +
  all-complete), TS-34 (multi-group M:N), and TS-35 (multi-license PSV +
  renewal reset). Must pass `npm run lint`, `npx tsc --noEmit`, `npm run
test`, `npx prettier --check .`, and the touched Playwright specs. Both
  additive migrations must be applied to repo + hosted and reflected in
  `src/integrations/supabase/types.ts`.
- **TE-11 — Sidebar authorization: the org-zone "Providers" item points at the
  E1.3 roster surface (amendment, 2026-07-15, independent review session per the
  PM request + REVIEW-HANDOFF).** TE-8 authorized only the wizard section, its
  feature components, and the Account-Detail summary — NOT a shell edit — and
  `docs/redesign/uiux-component-guide.md` makes a redesign epic's §5 the ONLY
  authorization to touch `src/components/layout/*`. This enabler adds that
  missing authorization: **`src/components/layout/Sidebar.tsx` (a protected
  `src/components/layout/*` file) may be edited so the org-zone "Providers" child
  navigates to `/onboarding/wizard?section=providers`** — the reviewed E1.3
  Provider Roster wizard section (F1.3.1/F1.3.2), reached via E1.0's `?section=`
  deep-link, which scrolls to and focuses `#wizard-providers-heading` — replacing
  that item's reserved `/soon` mapping. **Scope — Providers only; this supersedes
  ONLY the Providers half of E0.9 F0.9.3.** F0.9.3's acceptance criterion
  "Facilities / Providers remain reserved → `/soon?title=…`" is amended for
  Providers alone. Two invariants are explicitly preserved: (a) **Facilities
  stays reserved → `/soon?title=Facilities`** (a live Facilities destination is
  out of scope for this amendment); and (b) the legacy flat **`/providers`
  work-view route stays direct-URL-only** — no nav entry, still intentionally
  orphaned-but-URL-reachable per E0.9 F0.9.6 (registered in TECH-DEBT.md). The
  nav "Providers" item is the wizard section, never `/providers`. Nothing else in
  E0.9 F0.9.3 changes (the switcher tile, lifecycle grouping, active/hover/focus
  states, and every other TS-22 assertion stand). The implementing PR
  (currently #159) is the authorized consumer: it must cite this enabler (E1.3
  TE-11) in the `Sidebar.tsx` header comment and in `e2e/sidebar-ia.spec.ts`,
  keeping E0.9's provenance for the rest of the IA.
- **Likely tech debt to contain.**
  - The register flags `providers.license_*` columns as a **dead mirror** of
    `state_licenses` (the child table is the truth) and `providers.launch_id`
    as dead. E1.3 must read/write licensure only through `state_licenses`
    (+ the new PSV columns); do not resurrect the `providers.license_*`
    scalar mirror.
  - `providers.malpractice_*` is a register-flagged sprawl target (planned
    `provider_insurance_policies` child). E1.3 writes the existing flat
    malpractice columns (in scope) and must **not** introduce that child
    table here.
  - The protected `Provider` interface / `ProviderInput` may need additive
    widening for any CAQH field not yet typed; extend additively, never
    rename/remove.
- **No open questions for the PM.** The R1 provider decisions (CAQH baseline
  minus SSN/work-history/disclosures, M:N required at entry, PSV trail +
  renewal reset, SSN last-4 only, no status picker) are RESOLVED in
  `CLARIFICATIONS_NEEDED.md` (2026-07-10). The only correction is TE-1
  (`gender` already exists), which the Devin section records authoritatively
  — not a contradiction that blocks review.
