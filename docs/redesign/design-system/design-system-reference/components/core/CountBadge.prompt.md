`CountBadge` is a small number chip for counts on nav items and grouped rows — forest when the count needs attention, gray when idle.

```jsx
<CountBadge value={3} active />
<CountBadge value={24} />
```

Fixed at 18px tall with tabular figures so columns of counts align. Keep to short numbers; for status use `Badge`.
