---
epic: e1.4
title: Provider–Facility Assignment
stage: 1
release: R3
status: draft
owner: devin # drafted with the PM in-session, 2026-07-11
reviewed: false
---

# E1.4 — Provider–Facility Assignment

## Purpose and Context

Payer applications ask "where does this provider actually practice" — the
practice-location section is on nearly every commercial form, and the browser
extension fills it from assignment data. Today `provider_facility_assignments`
exists in the schema (provider × facility unique, org-scoped, single primary
per provider enforced by E0.10) but there is no wizard surface to manage it,
and the one field Sowmya computes by hand today — **the date the provider
started at each location** — is not captured at all. E1.4 adds the Assignments
wizard section (the first of the E1.0 disabled previews to go live), scoped by
the locked R3 decisions: group-scoped facility picker, one primary location per
provider across all groups, hand-picked assignments (no auto-assign), and a
required start date per assignment.

Implements the R3 PM decisions (2026-07-11): picker limited to the provider's
group(s)' facilities; single primary even across groups; hand-pick every time;
per-location start date is a tracked data point; no unassigned resting state —
every provider needs a group and at least one location.

## Personas Empowered

- **P1 Credentialing Manager** — assigns providers to locations while
  onboarding; the wizard actor.
- **P2 Credentialing Coordinator / Specialist** — reads assignments when
  filling payer forms (practice-location sections).
- **P10 Payer Portal / System Boundary** — consumes assignment data via the
  extension's practice-location fill.

## Component & Data Constraints

- Built from the current component library per
  [uiux-component-guide.md](./uiux-component-guide.md); scenario data from
  [seed-universe.md](./seed-universe.md).
- Implementation dependency: `provider_facility_assignments` exists
  (`id, org_id, provider_id, facility_id, created_at`; unique
  `(provider_id, facility_id)`; E0.10 enforces NOT-NULL + single `is_primary`
  per provider). E1.4 adds a `start_date` column (additive migration +
  table-register update).
- Implementation dependency: E1.3's `provider_group_assignments` determines
  which groups a provider belongs to; the facility picker derives from those
  groups' facilities (`facilities.group_id`).
- Date picker component: shared enabler (also needed by E1.3); if not yet
  built, this epic's section 5 must name it as an enabler, not an epic-local
  widget.

---

## Features

### F1.4.1 — Assignments wizard section

- **Description:** The E1.0 "Assignments" preview becomes an active wizard
  section listing every provider in the org with their assigned facilities and
  primary flag. Section progress is derived: Complete when every provider has
  ≥ 1 facility assignment; incomplete providers are listed first with an
  "Assign locations" affordance.
- **Persona:** P1.
- **Benefit hypothesis:** One place to see and close the "who works where" gap
  before readiness/case generation; replaces tribal knowledge.
- **Acceptance criteria:**
  - Section appears in the wizard after Providers, enabled (no longer a
    disabled preview).
  - Lists providers with facility chips; providers with zero assignments are
    flagged and sorted first.
  - Progress chip derives from data (no stored completion flags, E1.0 rule).
  - Empty state when the org has no providers points back to the Providers
    section.
- **Test/data scenario(s):** TS-39.

```gherkin
Feature: Assignments wizard section derives completeness
  Scenario: Provider with no location keeps the section incomplete
    Given Tree Hill Sports Therapy has two providers and one facility
    And only one provider has a facility assignment
    When P1 opens the Assignments wizard section
    Then the unassigned provider is listed first with an "Assign locations" action
    And the section shows In progress, not Complete
```

### F1.4.2 — Group-scoped assignment editor with per-location start date

- **Description:** Assigning locations for a provider shows only facilities
  owned by the provider's group(s) (never the whole org). Each selected
  facility requires a **start date** (date the provider began practicing
  there). Assignments are always hand-picked — no bulk/auto-assign in R3.
- **Persona:** P1.
- **Benefit hypothesis:** Matches contracting reality (a therapist practices
  at her group's clinics) and captures the start date Sowmya currently
  reconstructs by hand per provider per location for payer forms.
- **Acceptance criteria:**
  - Facility picker lists only facilities where `facilities.group_id` is one
    of the provider's assigned groups; other org facilities never appear.
  - Each assignment requires `start_date` before save.
  - Unchecking a facility removes the assignment (archive/history concerns are
    out of scope for R3 — assignments are current-state plus start date).
  - No "assign to all" bulk action.
- **Test/data scenario(s):** TS-39, TS-40.

```gherkin
Feature: Facility picker is scoped to the provider's groups
  Scenario: Two-group org shows only the provider's group's clinics
    Given Shelby Sports Rehab has Group 1 with one facility and Group 2 with two facilities
    And a provider assigned only to Group 1
    When P1 opens the assignment editor for that provider
    Then only Group 1's facility is offered
    And saving requires a start date for the selected facility
```

### F1.4.3 — Single primary practice location

- **Description:** Exactly one assignment per provider is marked primary —
  across all groups (payer forms ask for one primary practice location). The
  first assignment defaults to primary; changing primary is a one-click swap.
- **Persona:** P1, P2 (reads primary when filling forms).
- **Benefit hypothesis:** Deterministic answer for every form's
  "primary practice location" field; the DB already enforces the invariant.
- **Acceptance criteria:**
  - First saved assignment for a provider is automatically primary.
  - Marking another assignment primary demotes the previous one atomically.
  - A provider with assignments always has exactly one primary (E0.10
    constraint surfaces as a friendly error if violated).
  - Removing the primary assignment while others exist prompts to pick a new
    primary.
- **Test/data scenario(s):** TS-40.

```gherkin
Feature: One primary location per provider across groups
  Scenario: Primary swap when provider works in two clinics
    Given a provider with two facility assignments where Clinic A is primary
    When P1 marks Clinic B as primary
    Then Clinic B is primary and Clinic A is not
    And the provider still has exactly one primary assignment
```

---

## Scenario/Seed Data Mapping

- TS-39 — Assignment gap surfaced (Tree Hill: one assigned, one unassigned
  provider; section In progress).
- TS-40 — Two-group primary swap (Shelby: multi-group provider, group-scoped
  picker, start dates, primary swap).

## Dependencies

- E1.0 wizard section framework (merged); E1.2 facilities; E1.3 roster +
  `provider_group_assignments`.
- Additive migration: `provider_facility_assignments.start_date date` +
  table-register row update.
- Shared date-picker component (enabler shared with E1.3).

## Out of Scope / Non-Goals

- Auto-assign / bulk assignment rules (R5 scale pack).
- Assignment end dates / historical assignment records (current-state + start
  date only in R3).
- Cross-group facility coverage (picker is group-scoped by decision).
- Location-launch readiness (separate future epic).

## Revision History

| Date       | Change                                                                                                                                                                                                                                                                                                                                                                       | Author |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 2026-07-11 | Initial draft from R3 PM Q&A (Q1–Q5)                                                                                                                                                                                                                                                                                                                                         | devin  |
| 2026-07-12 | Independent review (REVIEW-HANDOFF): authored §5 TE-1–TE-11 + redlines — corrected the phantom `start_date` migration (column already in the baseline; no migration in this epic), native `type="date"` replaces the undefined date-picker enabler, authoritative table trace incl. `audit_log`, demote→promote primary-swap contract, wizard-registry activation authorized | claude |

---

## 5. Technical Considerations & Enablers

_Independent reviewer-owned (Claude Code, per REVIEW-HANDOFF). Every claim
verified against the baseline schema
(`20260704210000_baseline_live_schema.sql`), the E0.10 constraint migrations,
the table register, and the live assignment services on 2026-07-12._

- **TE-1 — Schema audit: `provider_facility_assignments.start_date` ALREADY
  EXISTS — this epic ships NO migration.** The baseline defines the table
  with `id`, `org_id`, `provider_id`, `facility_id`, `created_at`,
  `is_primary`, `start_date`, and `practice_frequency` (baseline lines
  268–277); the generated `src/integrations/supabase/types.ts` already
  carries `start_date`/`practice_frequency`, and the extension profile
  endpoint already selects both (`PROFILE_ASSIGNMENT_COLUMNS` in
  `src/services/providerProfile.ts`) — so wizard-captured start dates flow to
  the extension's practice-location fill with zero endpoint work. The epic
  body's "E1.4 adds a `start_date` column (additive migration + table-register
  update)" and the Dependencies migration line are therefore inaccurate; this
  note is the authoritative correction (the E1.3 TE-1 `gender` pattern — body
  text left unedited per the reviewer/author split). The locked "required
  start date" ([stage-3], 2026-07-11) is enforced at the **UI/service
  boundary, NOT as a DB NOT NULL**: every existing row is NULL there, and the
  legacy writers (`assignProviderToFacility`, the `createProviderWithDetails`
  facility bundle, the CSV import) write no start_date — a DB constraint would
  break them all. A future `CHECK (start_date IS NOT NULL) NOT VALID` per the
  E0.10 pattern is available once legacy writers thread the field; out of
  scope here. No DDL → no table-register change and no types regen required
  (the register's pfa row already records the E0.10 enforcement).
- **TE-2 — The DB invariants are already in place; lean on them.**
  `UNIQUE (provider_id, facility_id)` (baseline line 824), E0.10 NOT-NULL
  CHECKs on `provider_id`/`facility_id` (`20260710150000`), and
  `uq_provider_facility_assignments_one_primary` (`20260710170000`) — a
  partial unique ON `(provider_id) WHERE is_primary`, keyed by provider, not
  (provider, group), so F1.4.3's "one primary even across groups" is
  **already DB-enforced**. `src/lib/dbErrors.ts` already maps all four
  constraint names to domain messages ("This provider already has a primary
  facility.", …) — route every write through `translateDbError` and the
  F1.4.3 "friendly error" AC is met with zero new mapping. **Primary swap
  "atomically":** the browser PostgREST path has no transaction; sequence
  DEMOTE the current primary, then PROMOTE the new one (the reverse order
  trips the partial unique). A failure between the two UPDATEs leaves a
  provider with zero primary — DB-legal (the index only forbids two) — so the
  editor must detect and re-prompt (the same UI path as F1.4.3's
  removed-primary prompt). If the PM wants literal atomicity, a
  SECURITY DEFINER swap RPC is the escalation; not required for R3.
- **TE-3 — Service layer: extend the existing assignment writers, don't
  fork.** `src/services/launches.ts` already has `listFacilityAssignments`
  (org-scoped `select("*")` → `camelizeRow`) and `assignProviderToFacility`
  (upsert `ignoreDuplicates`, audited). E1.4 adds — in the same service or a
  dedicated `src/services/facilityAssignments.ts` — `createAssignment`
  (`{providerId, facilityId, startDate, isPrimary}`), `updateAssignment`
  (start-date edit; primary demote/promote per TE-2), and `removeAssignment`
  (a real DELETE — the RLS DELETE policy exists and pfa is NOT append-only).
  Every mutation: `org_id` from `requireActiveOrg()`, `snakeizeRow` at the
  boundary, `translateDbError`, and `writeAudit`. No E0.10-constrained state
  column is written, so `normalizeStateCode` does not apply here. Keep
  `assignProviderToFacility`'s signature stable for its existing callers
  (launch `AssignProviderDialog`, `importCommit.ts`) — thread optional
  `startDate`/`isPrimary` params rather than breaking them. Hooks: extend
  `src/hooks/useLaunches.ts` (or a new `useFacilityAssignments.ts`) on
  `queryKeys.facilityAssignments(orgId)`; mutations invalidate the
  `["facility-assignments", orgId]` prefix the existing hooks already use.
- **TE-4 — Additive type widening (protected file, additive-only).**
  `FacilityAssignment` in `src/types/index.ts` (lines 304–311) lacks
  `startDate`/`practiceFrequency` even though `listFacilityAssignments`
  already returns both via `select("*")`. Add `startDate: string | null`
  and, if the PM pulls it in (question 5), `practiceFrequency: string | null`.
  Never rename, remove, or retype existing fields.
- **TE-5 — Wizard registry activation + derived progress (authorized).** This
  section authorizes converting E1.0's DISABLED "Assignments" preview into an
  active section of the ordered registry in
  `src/routes/onboarding.wizard.tsx` — the first R3 preview to go live. It
  supersedes E1.0 TE-4's "all four R1 sections complete → show the
  Assignments preview": the next-action chain now extends into Assignments.
  Build gate: the route today is still the E0.8 placeholder page and
  `src/lib/onboardingProgress.ts` does not exist — E1.0 (framework) and
  E1.1–E1.3 (sections + `provider_group_assignments`) must merge first. Add
  an assignments resolver to `onboardingProgress.ts`: with ≥1 provider,
  `complete` when EVERY provider has ≥1 assignment, `in_progress` when some
  do, `not_started` when none do; zero providers renders the F1.4.1 empty
  state pointing at the Providers section and stays out of the completion
  math. Pure, unit-tested truth table; derived only — NO stored wizard flags
  (E1.0 rule). Unassigned-first ordering is a pure, tested sort.
- **TE-6 — Group-scoped picker derivation (hard E1.3 dependency).** A pure
  `src/lib/` helper (tested): eligible facilities = org facilities whose
  `group_id` ∈ the provider's `provider_group_assignments` group ids.
  `provider_group_assignments` is an E1.3 migration NOT yet in the repo —
  restated as a build gate, not just a Dependencies line.
  `facilities.group_id` is nullable (FK `ON DELETE SET NULL`): group-less
  legacy facilities never appear in the picker, which correctly implements
  "other org facilities never appear"; E1.2 requires the group at facility
  save, so wizard-created facilities always qualify. Do NOT fall back to the
  org-wide facility list on an empty result — render the honest empty state.
- **TE-7 — Components: native date input + checkbox rows; NO date-picker
  enabler exists.** Correcting the body: "Shared date-picker component
  (enabler shared with E1.3)" names an enabler no epic defines — E1.3 §5 has
  no date-picker TE, `src/components/ui/` has no date-picker/calendar/popover
  primitive, and the house date-field precedent is the native
  `<Input type="date">` (`src/components/providers/ProviderFormSections.tsx`,
  `LaunchEditModal.tsx`). Use the native date input; no new primitive, no new
  dependency. The assignment editor is a `Dialog` with one checkbox row per
  in-scope facility (checkbox/input/label/dialog primitives all exist); each
  checked row exposes its required start-date input plus the primary
  selector. This is deliberately NOT the E1.1/E1.2/E1.3 shared dropdown
  multi-select — chips-in-a-dropdown cannot host per-item date fields. If the
  composed row-list becomes a reusable unspecced component, log it as a
  `DESIGN-DEBT.md` row in the build PR (AGENTS.md component governance);
  never an epic-local one-off. Inline validation under each field; no
  placeholder-only labels.
- **TE-8 — Fixes the two standing warts; don't regress them into new code.**
  CLAUDE.md known warts: (a) provider edit drops facility assignments —
  `providers.$id.edit.tsx` passes `facilityIds: []` and the update path never
  syncs; (b) `is_primary` is read (NewCaseModal facility default) but never
  written by the app. E1.4's editor becomes the canonical assignment write
  surface: the first saved assignment defaults `is_primary = true`, swaps run
  TE-2's demote→promote, and every wizard write carries `start_date`.
  Provider edit continues NOT to touch assignments — do not "fix" wart (a) by
  re-syncing `facilityIds` inside `updateProviderWithLicenses`, which would
  silently delete rows now carrying `start_date`/`is_primary`. NewCaseModal's
  primary-based facility default starts receiving real primaries with zero
  changes there.
- **TE-9 — Schema/RLS/grants assessment + authoritative table trace.** No new
  table, column, policy, RPC, or grant: the four org-scoped RLS policies
  (member SELECT; specialist/admin INSERT/UPDATE/DELETE — baseline lines
  1032–1035) and the `authenticated` grants (baseline line 1165) already cover
  every E1.4 write, including removal. No anon-reachable surface. Append-only
  tables untouched except `audit_log` inserts via `writeAudit`. Authoritative
  trace (the Dependencies section omits one; AGENTS.md requires it): **reads**
  `providers` (keep the PHI-safe `PROVIDER_LIST_COLUMNS` projection — the
  section lists every provider; never widen to `select("*")`), `facilities`,
  `provider_groups`, `provider_group_assignments` (E1.3), and
  `provider_facility_assignments`; **writes**
  `provider_facility_assignments` (INSERT/UPDATE/DELETE) and `audit_log`
  (append-only inserts). Reconciled against
  `docs/data-model/table-register.md` (pfa: party/core, E0.10-enforced).
- **TE-10 — Design system & accessibility.** Shadowless white cards, token
  border `#E8E5E0` 1px, panel radius; derived status chips via the conformed
  `StatusPill` tint+ink pairs with text labels (never color-only); facility
  chips on provider rows are neutral tag chips (the sanctioned exception).
  `label`+control forms with inline validation; checkbox rows and date inputs
  keyboard-reachable with visible focus; Radix dialog focus trap preserved;
  usable at 1280px and 1024px. Icons lucide-react 16px inline.
- **TE-11 — Verification gates.** Unit-test the pure pieces: the assignments
  progress-resolver truth table, the unassigned-first sort, the group-scoped
  choice derivation, and the demote→promote swap planner (incl. the
  zero-primary recovery path). e2e for TS-39 (Tree Hill gap surfacing → In
  progress, unassigned provider listed first) and TS-40 (Shelby group-scoped
  picker + required start dates + primary swap) through the existing mock
  harness. Must pass `npm run lint`, `npx tsc --noEmit`, `npm run test`,
  `npx prettier --check .`, and the touched Playwright specs. No migration →
  no `types.ts` regen and no hosted apply in this epic.
- **Likely tech debt to contain.**
  - Legacy assignment rows and writers carry NULL `start_date` (launch flow,
    CSV import, provider-create bundle). PM questions 1–2 decide their
    treatment; until then the editor must render "start date missing"
    honestly, never fabricate a value.
  - Raw `["facility-assignments", orgId]` key literals in `useLaunches.ts`,
    `providers.new.tsx`, and `admin.import.tsx` duplicate
    `queryKeys.facilityAssignments`; E1.4 reuses the shared key and must not
    add a fourth literal — consolidation is a focused follow-up.
  - `practice_frequency` exists on the table and already rides the profile
    endpoint, but no app surface writes it (PM question 5); don't strand it
    silently.
  - `listFacilityAssignments` is an org-wide `select("*")` — acceptable at R3
    scale; revisit if assignment counts grow.

### Reviewer redlines (independent review per REVIEW-HANDOFF, 2026-07-12)

1. **blocker (resolved by TE-1)** — the epic's only claimed schema change is
   false: `provider_facility_assignments.start_date` already exists in the
   baseline (lines 268–277, alongside `is_primary` and `practice_frequency`),
   is already in the generated types, and is already selected by
   `PROFILE_ASSIGNMENT_COLUMNS` (`src/services/providerProfile.ts`). A builder
   following the body would ship a phantom migration and a false
   table-register claim. TE-1 is the authoritative correction: no migration
   in this epic; start_date is UI/service-required only.
2. **should-fix (resolved by TE-7)** — "Shared date-picker component (enabler
   shared with E1.3)" references an enabler no epic defines: E1.3 §5 names no
   date picker, `src/components/ui/` has no such primitive, and the house
   precedent is the native `type="date"` input
   (`src/components/providers/ProviderFormSections.tsx`).
3. **should-fix (resolved by TE-9)** — the Dependencies section lists no table
   trace (AGENTS.md requires one) and omits the `audit_log` writes every
   assignment mutation performs via `writeAudit`
   (`src/services/launches.ts`) — the standing correction from the E1.1
   review.
4. **should-fix (resolved by TE-2)** — F1.4.3's "demotes the previous one
   atomically" promises what the browser PostgREST path cannot deliver (no
   client transaction); the achievable contract is demote→promote sequencing
   under `uq_provider_facility_assignments_one_primary`
   (`20260710170000_structural_constraints.sql`) with UI recovery of the
   transient zero-primary state.
5. **nit** — the Component & Data Constraints column list
   "(`id, org_id, provider_id, facility_id, created_at`)" omits `is_primary`,
   `start_date`, and `practice_frequency` (baseline lines 268–277) — the
   understatement that let the phantom-migration claim survive drafting.
6. **nit** — the RESOLVED `[stage-3]` clarification itself records "additive
   `provider_facility_assignments.start_date`" as the decision premise; the
   premise is stale for the same reason as redline 1. This reviewer may not
   edit `CLARIFICATIONS_NEEDED.md` — flagged for the PM (question 3).

**Open questions for the PM (non-blocking):**

1. **Legacy NULL start dates vs section completeness:** rows written by the
   launch flow, CSV import, and provider-create bundle have no `start_date`.
   Does "provider has ≥1 facility assignment" complete the section regardless,
   or does a missing start date hold it at In progress until backfilled?
2. **Do the legacy writers change in R3?** Should the launch
   `AssignProviderDialog` and `/admin/import` start collecting `start_date`,
   or does the required-start-date rule bind only the wizard editor for now?
3. **Annotate `[stage-3]`:** confirm the start-date decision stands as
   UI-required capture of the EXISTING column (no schema change) and record
   that correction on the clarification entry (reviewer cannot edit it).
4. **Last-assignment removal:** advisory gap (as drafted — F1.4.1 surfaces
   it) or blocked, mirroring E1.3's blocked last-group removal? The
   `[stage-3]` "every provider needs … at least one location" reads as intent,
   not a hard block; confirm advisory.
5. **`practice_frequency`:** the column exists and rides the extension
   profile endpoint — explicitly out of scope for R3, or captured in the
   editor alongside `start_date`?
