# Design-debt register

Process for shipping a component that the design system doesn't yet approve.
Devs never block on design review — every deviation instead leaves a row here.

## The rule

1. **Compose first.** If an approved primitive (or composition of them) fits, use it.
2. **If truly new:** add the stock shadcn primitive to `src/components/ui/`, styled by
   **tokens only** — no custom colors, radii, shadows, or type outside the token set.
3. **Log it below** in the same PR (component, where used, why, owner, date) and note
   it in the PR description.
4. **Design review triages the register** each cycle: `approved` (spec added to the
   design system) or `replace` (prescribed substitute + follow-up task).

An unlogged unapproved component is a review blocker; a logged one never is.

## Register

| Component / deviation                                                                                        | Where used                                                                                                                                                                                                                                | Why needed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Owner | Date       | Status         |
| ------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | ---------- | -------------- |
| Toast keeps `shadow-lg` (spec: none)                                                                         | `src/components/ui/sonner.tsx`                                                                                                                                                                                                            | E0.9 internal conflict: F0.9.2's AC and the token sheet keep the toast's soft shadow ("floating overlays keep theirs"; `--mp-shadow-lg` is annotated "toast"), while the reference `Toast.jsx` says border-only. Border + white surface + 6px radius applied; the shadow stayed pending a design ruling.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | PM    | 2026-07-10 | pending review |
| Switcher "recents" = active org only                                                                         | `src/components/layout/Sidebar.tsx` (100+ orgs)                                                                                                                                                                                           | The 100+-org recents-only rule needs a recency signal the app doesn't track; the only known-recent org today is the active one, so the list shows it plus search. Revisit when a customer nears that scale.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | PM    | 2026-07-10 | pending review |
| States multi-select (composition)                                                                            | `src/components/onboarding/StatesMultiSelect.tsx` (E1.1 Provider Group form)                                                                                                                                                              | The design system defines no multi-select/combobox primitive and F1.1.1 requires a real operating-states multi-select. Composed per the register rule from approved primitives only — `DropdownMenu` + `DropdownMenuCheckboxItem` over the canonical `US_STATES` list, token-styled (4px control radius, token border), selection shown as text on the trigger. No new primitive, no new dependency.                                                                                                                                                                                                                                                                                                                                                                                                    | PM    | 2026-07-12 | pending review |
| Tag-list input (composition)                                                                                 | `src/components/onboarding/TagListInput.tsx` (E1.2 facility languages offered / interpreter languages)                                                                                                                                    | Languages are open-ended values, so neither a closed-list multi-select nor any design-system primitive fits. Composed per the register rule from approved primitives only — `Input` + `Button` + removable `Badge` chips, token-styled. No new primitive, no new dependency.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | PM    | 2026-07-12 | pending review |
| Calendar + Popover primitives (stock shadcn)                                                                 | `src/components/ui/calendar.tsx`, `src/components/ui/popover.tsx` (E1.4 date capture)                                                                                                                                                     | E1.4 TE-6 mandates a real date input and names these as the missing primitives to add via the shadcn generator (registry unreachable from the sandbox, so the canonical sources were vendored verbatim). Stock shadcn, token-styled; deps `react-day-picker@9` + `@radix-ui/react-popover` are the canonical shadcn choices (justification per the no-new-deps rule recorded in the E1.4 PR).                                                                                                                                                                                                                                                                                                                                                                                                           | PM    | 2026-07-12 | pending review |
| DatePicker (composition)                                                                                     | `src/components/DatePicker.tsx` (E1.4 assignment start dates; shared enabler for future date fields)                                                                                                                                      | The canonical shadcn date-picker composition (Button + Popover + Calendar) over the two primitives above; plain ISO date value. Composed from approved primitives only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | PM    | 2026-07-12 | pending review |
| State select (composition)                                                                                   | `src/components/StateSelect.tsx` (org intake + contact/capture address, contract dialog; shared)                                                                                                                                          | Standardizes every free-text "State" field onto the mastered `US_STATES` list so an operator picks a valid two-letter code instead of typing one (typos / full names / bad casing were the data-error risk). Composed from the stock shadcn `Select` primitives, token-styled; no new primitive, no new dependency. The multi-state sibling is `StatesMultiSelect`.                                                                                                                                                                                                                                                                                                                                                                                                                                     | PM    | 2026-07-12 | pending review |
| Batch summary banner (composition)                                                                           | `src/components/work/NextBestActionQueue.tsx` (E2.3 post-generation landing)                                                                                                                                                              | E2.3 TE-9: no `alert` primitive exists in `src/components/ui/`, so the created/skipped run-summary banner is composed from card tokens (bordered `rounded-md` container) plus the approved `tabs` two-value batch/all-work toggle. No new primitive, no new dependency; swap to a real `alert` if the design system adds one.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | PM    | 2026-07-13 | pending review |
| Roster drop zone (composition)                                                                               | `src/components/import/RosterDropZone.tsx` (E3.0 both upload surfaces)                                                                                                                                                                    | E3.0 F3.0.3 requires a drag-and-drop zone with hover/active states; the design system defines no drop-zone primitive. Composed per TE-9 from a token-styled dashed `rounded-md` container + a hidden `<input type="file">` fallback (the legacy admin-import picker precedent); keyboard-reachable (`role="button"`, Enter/Space). No new primitive, no new dependency.                                                                                                                                                                                                                                                                                                                                                                                                                                 | PM    | 2026-07-13 | pending review |
| Scan progress bar (composition)                                                                              | `src/components/import/ImportRunPanel.tsx` (E3.0 import run panel)                                                                                                                                                                        | E3.0 TE-9 calls for "a progress element bound to staged/total" and no `progress` primitive exists in `src/components/ui/` (the shadcn generator registry is unreachable from the sandbox). Composed from two token-styled divs (`--mp-neutral-tint` track, primary fill) with `role="progressbar"` + aria values. Swap to the stock shadcn `progress` primitive if the design system adds one.                                                                                                                                                                                                                                                                                                                                                                                                          | PM    | 2026-07-13 | pending review |
| Section upload card (composition)                                                                            | `src/components/onboarding/SectionUploadCard.tsx` (E3.3 the per-section bulk uploader beside each wizard manual form)                                                                                                                     | E3.3 TE-10: the streamlined upload sits beside each section's manual form as a token-styled bordered card wrapping the E3.0 `RosterUploader`. When the TE-5 ladder is unmet (Facilities/Providers without a group) it renders a DISABLED placeholder — a token-styled dashed `rounded-md` box (mirroring the approved `RosterDropZone` dashed style) + a pointer to the Provider Group section — instead of an active drop zone. No new primitive, no new dependency.                                                                                                                                                                                                                                                                                                                                   | PM    | 2026-07-14 | pending review |
| Section import preview (composition)                                                                         | `src/components/import/SectionImportPreview.tsx` (E3.3 provider_group / facility staged-import review)                                                                                                                                    | E3.3 TE-8/TE-10: the group/facility preview reuses the established E2.0/E3.1 preview idiom (token-styled metric cards + `Collapsible` drill-downs + explicit Commit/Cancel) for the simpler skip-on-match grains — no conflict review, no batch-assign. Composed from approved primitives only (`Button`, `Collapsible`, card divs). No new primitive, no new dependency.                                                                                                                                                                                                                                                                                                                                                                                                                               | PM    | 2026-07-14 | pending review |
| Payer-pipeline badge + transition control (composition)                                                      | `src/components/cases/pipeline/*` (E4.0 case detail/list/queue)                                                                                                                                                                           | E4.0 renders the external pipeline state through the existing `StatusPill` (status-semantic tone, kept visually distinct from the internal credentialing pill — no new pill primitive) and drives transitions via a stock `dropdown-menu` + `dialog`/`select`/`input`/`textarea`/`DatePicker` composition (the `ChangeStatusDialog` modal idiom). No new primitive, no new dependency.                                                                                                                                                                                                                                                                                                                                                                                                                  | PM    | 2026-07-15 | pending review |
| Document store surfaces (composition)                                                                        | `src/components/documents/*` (E4.5 provider/group document tables, upload dialog, history dialog, case verification, expiring-credentials report)                                                                                         | E4.5 TE-10: dense document tables composed from the stock `Table` primitive with custom cell markup (kind + filename two-line cells, derived expiration pills via the approved `StatusPill`, icon actions with `Tooltip` + aria-labels); the upload form is a stock `Dialog` + `Select`/`Input`/`Label` + the approved `DatePicker`; a native `<input type="file">` (no drop-zone — the explicit-form rule) with the centralized MIME/size pre-flight. One-pixel borders, tokens only, no shadows/gradients/base64 previews. No new primitive, no new dependency.                                                                                                                                                                                                                                       | PM    | 2026-07-17 | pending review |
| Unified case-status control + evidence dialogs (composition)                                                 | `src/components/cases/CaseStatusControl.tsx`, `CaseStatusDialogs.tsx`, `CaseStatusPill.tsx`, `CaseStatusHistoryPanel.tsx` (E6.0)                                                                                                          | The design system defines no status-transition control; composed per the register rule from approved primitives only — DropdownMenu + Dialog + Select + DatePicker + the shared StatusPill tone map (fixed tint+ink pairs, 4px borderless). Supersedes the E4.0 payer-pipeline badge/control row's transition UI (those components were retired with the machine). No new primitive, no new dependency.                                                                                                                                                                                                                                                                                                                                                                                                 | PM    | 2026-07-19 | pending review |
| Cases pivot switcher + Finish-setup banner + Groups shell cards (compositions)                               | `src/routes/cases.index.tsx` (pivot Tabs), `src/components/org/FinishSetupBanner.tsx`, `src/routes/groups.tsx` (E6.1)                                                                                                                     | The merged Cases surface's three-pivot switcher is the stock shadcn `Tabs` two-value idiom (the E2.3 batch/all precedent) driving URL state; the Finish-setup banner is the E0.4 OnboardingBanner's card+button composition with a live wizard-derived next step; the Groups shell's link cards are token-styled bordered cards from approved primitives. No new primitive, no new dependency.                                                                                                                                                                                                                                                                                                                                                                                                          | PM    | 2026-07-19 | pending review |
| Group breadcrumb + fulfillment board row (compositions)                                                      | `src/routes/groups.$groupId.tsx` (breadcrumb nav), `src/components/groups/PayerNetworkBoardContent.tsx` (board row cards) (E6.2)                                                                                                          | The design system defines no breadcrumb primitive — composed as a plain `nav[aria-label=Breadcrumb]` of links + chevron icons, token-styled (the epic's "simple route-derived" call, no framework). The fulfillment board rows are the established row-card list idiom (bordered cards + the shared StatusPill tone map + stock `Collapsible` drill-downs); the buffer banner reuses the approved amber note-box tokens. No new primitive, no new dependency.                                                                                                                                                                                                                                                                                                                                           | PM    | 2026-07-19 | pending review |
| Generation grid + sticky reconciliation bar (composition)                                                    | `src/components/generation/GenerationGrid.tsx` (E6.3 — the `/generation` preview grid)                                                                                                                                                    | The decoupled grid is composed from approved primitives only — stock `Tabs` (pivot chips, the two-value idiom), `Table` with per-group bordered containers + header check-all `Checkbox`es, the shared `Badge`/StatusPill tone pairs for evidence chips, and `Collapsible` for run history. The always-visible reconciliation confirm bar is a token-styled bordered card pinned `sticky bottom-0` (no floating primitive exists; shadowless per the token sheet). The generic-fallback warning reuses the approved amber note-box tokens. No new primitive, no new dependency.                                                                                                                                                                                                                         | PM    | 2026-07-19 | pending review |
| Provider record identity form + roster gap pills (compositions)                                              | `GroupsFacilitiesPanel.tsx`, `EnrollmentsPanel.tsx`, `src/routes/providers.index.tsx` / `providers.$id.index.tsx` (E6.4; 2026-07-21 master-edit rework)                                                                                   | Identity is one master Edit → whole-form edit mode → one diff-only audited save (the E6.4 per-field `InlineField` pencil editor is RETIRED and deleted per user handoff 2026-07-21; DOB stays masked-at-rest, revealed in edit mode). Composed from approved primitives only (Input/Label/Button/StateSelect). The record's section jump-nav is a plain anchor `nav` (the E6.2 breadcrumb precedent); gap pills reuse the shared StatusPill tone map as roster-row links; the groups/facilities, licenses, and enrollments panels are the established chip + row-list + Dialog idioms. No new primitive, no new dependency.                                                                                                                                                                             | PM    | 2026-07-19 | pending review |
| Payer Setup two-tab shell + readiness funnel + in-editor form panel (compositions)                           | `src/components/payer-admin/PayerAdminTabs.tsx`, `PayerReadinessFunnel.tsx`, `src/components/templates/FormStepPanel.tsx`, `src/routes/admin.payer-admin.sops.tsx` (drift banners) (E6.5)                                                 | The two-segment tab shell is a plain link nav (real URLs, `aria-current`) styled on the stock Tabs look — no Radix tabs since the areas are routes; the governance note reuses the approved amber note-box tokens. The Ready-for-business funnel is the established hand-rolled admin table idiom + the shared StatusPill tone map. The form-step panel composes stock `Collapsible` + `Dialog` + `Select`/`Input`/`Button` + StatusPill into the register→train→dry-run loop (collapsed by default to protect the wizard's typing-latency budget); drift banners reuse the approved red error-box tokens. No new primitive, no new dependency.                                                                                                                                                         | PM    | 2026-07-19 | pending review |
| Grouped report index + report tables + at-risk/badge chips (compositions)                                    | `src/routes/reporting.index.tsx` (group sections + leads badge), `src/components/reporting/{LaunchesReport,DenialsReport,FacilitiesWithoutProvidersReport,LocationsPerGroupReport}.tsx`, `src/components/cases/AddTouchDialog.tsx` (E6.6) | The four-group index is plain `section` headings over the existing report cards; the new-leads badge and the launches at-risk flag are token-styled spans from the fixed warn tint/ink pair (the StatusPill tone map's tokens — no new colors). The reports are stock `Table` + `Tabs` (denials pivot, the two-value idiom) + the shared EmptyState/Skeleton; the Add-touch dialog composes stock `Dialog` + `Checkbox` list over the extracted shared AddTouchForm. No new primitive, no new dependency.                                                                                                                                                                                                                                                                                               | PM    | 2026-07-19 | pending review |
| CsvImportPanel — site-wide CSV-import disclosure (composition)                                               | `src/components/import/CsvImportPanel.tsx` (used by `SectionUploadCard`, `GroupFacilitiesContent`, `PayerNetworkBoardContent`) (2026-07-20)                                                                                               | The user-requested standardization of every CSV entry point on the group Payer Network board's collapsed-disclosure pattern: stock shadcn `Collapsible` with an icon-labeled trigger (Upload + ChevronDown, muted 12.5px text) expanding to the existing token-styled import box (instructions + template download + drop zone). One shared wrapper so the collapse behavior/styling can never drift again; deliberately collapsed even when a run is resumable, matching the reference surface. No new primitive, no new dependency.                                                                                                                                                                                                                                                                   | PM    | 2026-07-20 | pending review |
| Payer Setup single-view compositions — KPI filter cards, zero-state orientation, pagination footer (Slice A) | `src/components/payer-admin/PayerSetupPage.tsx` (payer-and-cases screen 1)                                                                                                                                                                | The design system defines no KPI-filter-card, stepper-orientation, or pagination primitive; the payer-and-cases bundle prototypes all three, rebuilt here from approved primitives + tokens only: the KPI cards are plain `button`s with `aria-pressed` (selected = primary fill, the prototype's exact treatment); the zero-payers three-step orientation is numbered token-styled circles + text; the pagination footer composes the stock `Select` (rows per page 5–100) + `Button` pager inside a `nav[aria-label=Pagination]` (numbered buttons ≤7 pages, Page-X-of-Y text beyond — the prototype renders unbounded buttons). Long state lists truncate to plain text via the existing `formatStates` (the interactive +N-more disclosure is design-removed). No new primitive, no new dependency. | PM    | 2026-07-27 | pending review |
| ID-capture row + Awaiting-ID pills (Slice D, composition)                                                    | `src/components/cases/CaseStatusDialogs.tsx` (`IdCaptureRow`), `src/components/providers/EnrollmentsPanel.tsx`, `src/components/groups/PayerNetworkBoardContent.tsx` (payer-and-cases screen 5)                                           | Screen 5's expectation-driven Approved dialog needs a per-ID capture row (payer-worded label + scope hint, mono value input, the "Didn't receive" escape with its amber Awaiting-ID note) — no design-system primitive covers it, so it is composed per the register rule from approved primitives only (`Label` + `Input` + `Checkbox`). The Awaiting-ID / No-ID-issued display states reuse the shared `StatusPill` tone map (amber/neutral) beside the existing case links. No new primitive, no new dependency.                                                                                                                                                                                                                                                                                     | PM    | 2026-07-27 | pending review |
| Template Editor v-chip + derived context banner + restore-as-new (compositions)                              | `src/components/templates/TemplateWizard.tsx` (v-chip, intent banner), `TemplateVersionHistory.tsx` (restore action), `PayerReadinessFunnel.tsx` (intent deep-links) (Slice F)                                                            | Payer-and-cases screen 4 (Template Editor): the header version chip is a token-styled mono span (no chip primitive exists beyond StatusPill, which is status-semantic); the readiness deep-link context banner is a token-styled bordered card with a 3px primary left rail (the design's derived-banner treatment, composed from card tokens — no alert primitive); History stays a stock `Dialog` (the app has no Sheet primitive — the provider-record "slide-over stays a Dialog" precedent) and gains a per-row outline `Button` restore action. No new primitive, no new dependency.                                                                                                                                                                                                              | PM    | 2026-07-27 | pending review |
| Searchable states multi-select (composition, Slice B)                                                        | `src/components/payer-admin/PayerStatesField.tsx` (payer-and-cases screen 2 — payer operating states)                                                                                                                                     | Screen 2 specifies a SEARCHABLE multi-select over all 50 states + DC, and the E1.1 `StatesMultiSelect` can't host one: it is a Radix `DropdownMenu`, which owns keydown for typeahead, so a search `Input` inside it never receives characters. Composed instead per the register rule from approved primitives only — `Popover` + `Input` + checkbox-styled option rows over the canonical `US_STATES` list, with a count chip and a Done affordance, token-styled (4px control radius, token border). No new primitive, no new dependency; the onboarding multi-select is untouched.                                                                                                                                                                                                                  | PM    | 2026-07-27 | pending review |
| Payer details form — ID-expectation rows + alias chips (compositions, Slice B)                               | `src/components/payer-admin/PayerDetailsForm.tsx`, `PayerNameStep.tsx` (payer-and-cases screen 2)                                                                                                                                         | The design system defines no checkbox-plus-conditional-field row and no chip editor. Both are composed from approved primitives only: each ID-expectation row is `Checkbox` + `Label` + a conditional `Input` (collapsing to a muted "Not issued"), and aliases are removable bordered chips + `Input` + `Button` (the `TagListInput` idiom, rebuilt locally because the payer-admin module boundary forbids importing `@/components/onboarding`). The near-match panel reuses the approved amber note-box tokens; status uses the shared `StatusPill`. No new primitive, no new dependency.                                                                                                                                                                                                            | PM    | 2026-07-27 | pending review |
| Case-detail header card + hinted status menu + task-step list (Slice E, compositions)                        | `src/components/cases/CaseHeader.tsx`, `CaseStatusControl.tsx` (`MenuMove`), `CaseTasksPanel.tsx` (inline steps), `CaseDetailsPanel.tsx` (payer-and-cases screen 6)                                                                       | Screen 6 needs four unspecced pieces and the design system defines none of them, so each is composed per the register rule from approved primitives only: the header is a token-styled `Card` carrying identity + tracking ID + the status control; the Update-status menu entries are stock `DropdownMenuItem`s with a second muted hint line (`MenuMove`); the single task list renders each task's execution type as a token-styled chip with its ordered steps beneath (dot + label, the current step's "Open step" `Button` opening the existing drawer); and Details is one `Card` with three labeled groups over `Separator`/`CopyButton`/the shared `StatusPill` (Awaiting-ID reuses Slice D's derivation). No new primitive, no new dependency.                                                | PM    | 2026-07-27 | pending review |
